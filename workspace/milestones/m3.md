# M3 — Core workflows and accurate data  *(not started, M2 is active)*

**Target:** Week 3
**Active milestone right now:** `m2.md`. This file exists early so M3-sized items found
during M2 have somewhere to go instead of inflating the active milestone.

**How to use this file:** pick a task, put your name in `Owner`, tick it when done. Keep
entries to one line — task only, no implementation notes. Technical detail belongs in
`workspace/claude/task-notes.md`, shared findings in `workspace/claude/context.md`.
Decisions that need someone's judgment call go in `workspace/decisions.md`, not here.
`git pull --rebase` before editing.

**Legend:** `[x]` done · `[~]` in progress · `[ ]` open

---

## Claim board (before editing a god-file)
Add a line before touching `server/routers.ts`, `server/db.ts`, or `drizzle/schema.ts`:
`<name>, <file>, until <time>`.
- *(none right now)*

---

## A. Protocol build
- [x] Handle client-buys vs we-ship correctly in the protocol build. `Owner: Saboor`
- [ ] Support 2 / 3 / 6-month protocol lengths. `Owner: ___`
- [ ] Remove the remaining Program dependencies: the assignment panel on a client's record, the program-phase template sync in ProtocolsTab, and the phase journey on the client protocol page (which also feeds the PDF and the protocol email). `Owner: ___` (the admin page and write endpoints already went in M2; this is the half that changes what live clients see — and Jason has gone from "remove it" to "help me understand it", so confirm before starting)
- [x] Fix protocol pricing/totals accuracy. `Owner: Saboor`
- [ ] Fix inaccurate product prices. `Owner: ___`

## B. Orders, fulfillment and payments
- [~] Client-facing and internal notes on custom orders. `Owner: ___`
- [ ] Surface custom-order Internal Notes outside the Custom Orders view (e.g. on the client record), not just there. `Owner: ___`
- [ ] Let staff view and change the shipping address at checkout, and let the client change it too — for this shipment only or permanently. `Owner: ___`
- [~] Packing-slip insurance amount. `Owner: ___`
- [ ] Build a fulfillment-friendly packing slip: address, shipping instructions, stocked items only, no membership fee, insurance kept off the slip, invoice access. `Owner: ___` (Kari's ask, master doc)
- [ ] Break down drop-ship vs Omega-stocked items, with tracking per drop-ship vendor. `Owner: ___`
- [ ] Notify the team when a custom order is paid and ready to ship. `Owner: ___`
- [ ] Verify payment history is accurate; fix Payment Mode. `Owner: ___` (Jason: "I can already tell Payment Mode doesn't work")
- [ ] Fix inventory accuracy. `Owner: ___`

## C. Check-ins
- [ ] Consolidate the client check-in from 8-10 screens down to a few. `Owner: ___`
- [ ] Surface progress photos on the check-in screen. `Owner: ___`

## D. Templates and workflows
- [ ] Let a project's tasks/subtasks be added or removed from the Master template. `Owner: ___` (Lisa's ask)

## E. Next from Jason (review doc, 2026-08-05)
From Jason's screen-by-screen walkthrough. **Read his wording before starting one** —
`assets/jason-review-2026-08-05.pdf` plus the master doc. Full mapping of every comment to
a screen: `task-notes.md#jason-review-2026-08-05`.

- [ ] Per-category "client buys": mark a whole category as client-buys and have it apply to everything in it, like the existing check all / uncheck all. `Owner: ___` (master doc asks for this at line-item level; the review doc escalates it to categories)
- [ ] Fix the non-discountable flag so ND items actually escape discounts. `Owner: ___` (the flag is stored 0/1 but compared against `false`, so the check never matched — see `shared/flags.ts`. Was parked pending sign-off because it raises client totals; **that blocker is void, the database is all test data**)
- [ ] Make My Metrics pull weight and body-fat from the weekly check-ins clients already fill in. `Owner: ___`
- [ ] Review Site Settings and cut it to what's actually needed. `Owner: ___` (master doc)
- [ ] Rebuild the protocol PDF. `Owner: ___` (blocked: Jason says they're "ugly and don't give all the info" but has never specified what's missing — nothing about PDFs appears in the master doc, so this needs a spec from him before it can be sized)

---

## Handoff to testing
- [ ] Flag each area to Jason's team as it goes live, using their bug-report format. `Owner: ___`

---

**Net for M3:** this is the milestone where the app has to become *correct* rather than
just tidy — pricing, inventory, payment history and the fulfilment paperwork the team
actually works from. Most of it touches money or stock, so unlike M2's removals these need
verifying against real behaviour, not just a "nothing else references this" check.
