# Current Milestone — M2: Declutter + Chat

**Target:** Week 2 (~50h Farjad billed + planning, Saboor full-time)
**Started:** 2026-08-04
**Previous milestone:** `m1.md` (closed, a few items parked there on purpose — don't
pull them in here).

**How to use this file:** pick a task, put your name in `Owner`, tick it when done. Keep
entries to one line — task only, no implementation notes. Technical detail belongs in
`workspace/claude/task-notes.md` (Claude-only reference, messy is fine there) or
`workspace/claude/context.md` (shared findings); link to it if a task needs one.
Decisions that need someone's judgment call go in `workspace/decisions.md`, not here.
`git pull --rebase` before editing.

**Legend:** `[x]` done · `[~]` in progress · `[ ]` open

---

## Claim board (before editing a god-file)
Add a line before touching `server/routers.ts`, `server/db.ts`, or `drizzle/schema.ts`:
`<name>, <file>, until <time>`.
- *(none right now)*

---

## A. Removals — Jason's go list
- [x] Remove unused admin pages (AuditLogs, ContactAdmin, DataIntegrityAudit, EmailEngagement, OnboardingManager). `Owner: Saboor`
- [x] Drop 14 dead database tables; archive 33 dead scripts. `Owner: Saboor`
- [x] Trim the Launchpad hub page to Jason's keep list; send Trusted Partners and Coaching Plans out to omegalongevity.com. `Owner: Farjad`
- [ ] Remove Notification Analysis (Jason 2026-07-31: developer reference docs, not an admin tool — see `decisions.md`). `Owner: ___`
- [x] Remove the Programs admin page and its write endpoints. `Owner: Farjad` (notes: `task-notes.md#m2-removals`)
- [~] Remove the remaining Program dependencies: the assignment panel on a client's record, the program-phase template sync, and the phase journey on the client protocol page. `Owner: ___` (this is the M3 "Remove Program dependencies from the protocol build" task; it changes live client protocols and PDFs, so it was not folded into the page removal)
- [x] Remove the Masterclass Videos admin page. `Owner: Farjad` (notes: `task-notes.md#m2-removals`)
- [ ] Send the masterclass out to GHL: retire the client masterclass page and the Protocol Build masterclass tab. `Owner: ___` (blocked: nobody has the GHL masterclass URL)
- [x] Remove the Affiliate Partners admin page. `Owner: Farjad` (notes: `task-notes.md#m2-removals`)
- [x] Remove Daily Tools, including both pages under it. `Owner: Farjad` (notes: `task-notes.md#m2-removals`)
- [x] Remove payment reminders from the protocol build. `Owner: Farjad` (notes: `task-notes.md#m2-removals`)
- [ ] Remove redundant/duplicate views across the admin app. `Owner: ___`

## B. Chat & messaging
- [x] Fix chat formatting (spacing and lists). `Owner: Saboor`
- [ ] Support editing a sent chat message. `Owner: ___` (one shared backend, five UI surfaces — read `context.md` first)
- [ ] Support deleting a sent chat message, coach or client side. `Owner: ___` (moved from M1, 2026-08-01)
- [ ] Fix chat photo uploads. `Owner: ___`
- [ ] Basic to-do / action-item capability. `Owner: ___`

## C. Client protocol page overhaul
- [ ] Audit the whole client protocol page, cut what's redundant or unused, tidy the layout — same treatment the client dashboard got in M1. Second most-visited client page. `Owner: Farjad`

## D. Content & email
- [ ] Export the peptide cheat sheet as an image; link out to Omega Elite. `Owner: Farjad` (blocked: needs the screenshot export from Farjad)
- [ ] Simplify email branding, and decide the fate of Email → Preview alongside it. `Owner: ___` (see `decisions.md`)

## E. People & roles
- [~] Finish the Team → People rename and per-person roles. `Owner: ___` (the `phase4-people-rename.sql` timing question is open in `decisions.md`; until it runs, `personId` isn't a real column — raw SQL referencing it fails silently)

## G. Admin dashboard restructure (Saboor + Jason, 2026-08-03)
It reads as 13 stacked sections with no hierarchy, three things doing the same job, and no
charts. Reshaping it into: metrics on top, one work queue, analytics side-by-side, actions
out of the page body. Admin keeps its orange accent — the brand-scope decision stands.
- [x] Add one combined dashboard-metrics query so the page doesn't scan all payments twice. `Owner: Saboor`
- [x] Metrics strip: 4 compact tiles on top. Revenue gets a real 6-month sparkline; the other three get a number plus a comparison only where one honestly exists (no faked trend lines on point-in-time counts). `Owner: Saboor`
- [x] Merge Today's Tasks + Awaiting Follow-Up + the overdue alert + Unmapped Items into one "Needs attention" queue sorted by urgency. Changes behaviour and retires 3 widget keys — agreed. `Owner: Saboor`
- [x] Move the create actions into a single header menu; drop the Quick Actions section and the 3 static explainer boxes in Protocol Collaboration Center. `Owner: Saboor`
- [x] Put the two email widgets and the enrollment funnel side by side instead of stacked full-width. `Owner: Saboor`
- [x] Fix the Customize panel: it doesn't scroll so the last widgets are unreachable; `enrollmentPipeline` is missing from the list so two big sections can never be switched off; the drag handle implies reordering that was never wired up (the server already has `updateOrder`); and the text is left/right/centre aligned in one narrow panel. `Owner: Saboor`

## H. Next from Jason (review doc, 2026-08-05)
Jason walked the deployed app screen by screen and commented next to screenshots. **Read
his actual wording before starting one of these** — `assets/jason-review-2026-08-05.pdf`,
and the master doc alongside it. Half his comments say "this" and only make sense next to
the image. Full mapping of every comment to a screen, plus which claims check out against
the master doc: `task-notes.md#jason-review-2026-08-05`.

- [x] Point the "View Coaching Plans" links on the public home page at omegalongevity.com. `Owner: Farjad` (all 3 now go to omegalongevity.com/coaching-paths)
- [ ] Jason also said to "discard the old page that was there" (`/transformation`). Not done with the links: that page is the live enrollment funnel, with checkout, verify and masterclass routes under it, so retiring it is a separate decision rather than a link change. `Owner: ___`
- [ ] Install-app prompt: restyle to brand, add "don't show this again", and add a permanent "Install App on Phone" link for people who dismissed it. `Owner: ___`
- [x] Finish the Launchpad trim — removed "Why Omega Longevity" and "How It Works", kept Featured Podcast Guest as he asked. `Owner: Farjad`
- [ ] Launchpad "Ready for Real Results?" banner: master doc says the coaching-plans CTA should be "less pronounced" (it already links out to omegalongevity.com/coaching-paths). Left as-is because "less pronounced" is a design call, not a removal. `Owner: ___`
- [x] Launchpad hero is left-aligned, should be centred. `Owner: Farjad` (it was the "What's Inside" cards, not the hero — 2 live cards left in a 3-column grid after Omega Store was hidden)
- [ ] Launchpad testimonials: replace with a link to omegalongevity.com's testimonials page. `Owner: ___`
- [ ] Redirect `/partners` to omegalongevity.com/resources rather than deleting it — five email templates and the protocol PDF link to it, and clients already hold those emails. `Owner: ___`
- [ ] Coaching Sessions page should go to the Calendly link. `Owner: ___`
- [x] Remove My Inventory. `Owner: Farjad` (removed the dashboard tile, the Launchpad mobile-menu item, the route and the page — leaving the page routed with no entry point would just orphan it. The "skim My Documents down" half is untouched: Jason thinks it is in the master doc but it is not, so there is nothing to skim it against)
- [x] Client dashboard: "Omega Elite Community" card should read "Join or Login". `Owner: Farjad` (changed only the dashboard resources card he pointed at; the Launchpad card's "Join Community" button is a deliberate sales CTA for people who don't have it yet, left alone)
- [~] Fix the "Omega Longevity Peptide Coach" formatting. `Owner: Farjad` (his note had no screenshot, so this is a best read: the PWA `short_name` was the full 28-char name, which phones truncate on the home screen, and the PWA/tile colours were still pre-rebrand orange + slate. Confirm with Jason whether he meant somewhere else)
- [x] The blue "wait" overlay still uses pre-rebrand styling. `Owner: Farjad` (the route-transition `LoadingSpinner` in `App.tsx`, was slate-900 + orange-500)
- [ ] Settings crash: "An unexpected error occurred — cannot read properties of undefined (reading 'substring')". `Owner: ___`
- [ ] Address autocomplete: suggestions appear but picking one doesn't fill city/state/zip. `Owner: ___`
- [ ] Password reset email never arrives (`jason@sossupport.net`), and the welcome email doesn't either. Check the Railway startup banner first — if it says side effects suppressed, this is one env var, not SMTP. `Owner: Farjad` (investigating)
- [ ] Remove the Coaching Package and Coaching Price fields from the protocol form — never used, price already lives in the protocol. Check first: `coachingPrice` feeds protocol totals and the payment portal. `Owner: ___`
- [ ] Review the tab row on a client record (Coach Notes, Internal Notes, Intake Forms, Clone History, Progress, Check-In Summary, Check-In Settings, Notification History, Documents). Master doc covers check-ins, progress photos and notification history; the rest it does not. `Owner: ___`
- [x] Label "Copy Link" more clearly — now "Copy Protocol Link". `Owner: Farjad`
- [x] Decide the fate of "Reset Protocol Approval". `Owner: Farjad` (Jason answered in his reply: keep it, "highly beneficial". The inventory work is tracked in H2 below)

## H2. Next from Jason — part 2 (his reply to the review)
Source: `assets/jason-review-2-reply.pdf`, numbered 1-6. **Most of this is bigger than M2.**
It is parked here so it is tracked and so the two decisions it settles are visible; expect
the feature-sized items to move to M3 or later once Farjad has sized them. Read the PDF
before starting one, several points reference screenshots.

**Two decisions it settles:**
- [x] Programs: Jason has confirmed killing it. He built it for 3-4 clients, they stopped doing weekly check-ins, it drifted out of sync, it was too much work and he will not offer it again. Clients' goals changed through the year so the template was not followed anyway. `Owner: Farjad` (decision only — the removal itself is the M3 task in `m3.md`, now unblocked)
- [x] Reset Protocol Approval: **keep it**, he called it "highly beneficial". `Owner: Farjad` (decision only — the inventory improvement below is the work)

**Small and well-defined:**
- [ ] Make Reset Protocol Approval restore inventory. Feasible and contained: `inventory_transactions` already records every deduction against the protocol id with before/after quantities, the type enum already has `return`, and `clientProtocols.inventoryDeductedAt` is the flag to clear. `Owner: ___`
- [x] Rename the "Invited" badge on the Clients list — now "No login yet", with a tooltip explaining it does not affect protocol, payment or profile. `Owner: Farjad` **Not a bug** — verified our app has the same three-state logic (Linked / Invited / No Account) and the state Jason saw is legitimate: Paid and Completed are protocol fields, and Profile % is only shipping-address completeness, so none of them need an account. His screenshot was from the old peptidecoach.pro app but the same confusion applies here. The label is the problem. `Owner: ___` (one edge case that *would* be a real bug: the account lookup matches email exactly, so a client who signed up with a different address than the one on their protocol shows "Invited" despite having an account — checkable in the DB)

**Feature-sized, needs scoping before it can be committed to a milestone:**
- [ ] **"Client Journey Notes"** — his biggest ask. A timestamped notepad per *client* that he can write into from anywhere and that appears on every one of that client's pages, especially the check-ins. Not covered today: `coachNotes`, `internalNotes` and `notes_history` are all scoped to a **single protocol**, so by a client's third protocol the notes from the first two are invisible. That is exactly the pain he describes. ⚠️ The `journey_notes` table already in the schema is a **different feature** — it is the client's own private journal (mood, energy, sleep) shown on their dashboard, not a coach-facing note. `Owner: ___`
- [ ] Rework the client check-in flow. He says the old app's "client corner" was far superior despite its problems, and points at the Loom video for ideas. Overlaps the M3 check-in consolidation. `Owner: ___`
- [ ] Client-level standing terms: one input that sets long-term arrangements for a client as a whole rather than per protocol (his examples: 10% off all orders, monthly friends-and-family coaching instead of flagship). He compares it to contracts in his IT business. ⚠️ Raise with him before building: this is conceptually close to Programs, which he just asked us to kill. The difference is real (standing terms on the *person* vs a fixed template on the *protocol*) but worth naming so we don't rebuild what he removed. `Owner: ___`
- [ ] Give him protocol and intake context where he is already replying. His example: mid-chat a client asks about NAD+, and to answer he has to open a new browser window, open the client, open the Intake Form for her goals, then open and preview the Protocol to check whether NAD+ is in it. Probably the highest day-to-day value item here after the notes. `Owner: ___`
- [ ] Paste a screenshot straight into any chat. Currently the chat editor handles pasted Word HTML but ignores pasted images, so this does not work at all. Should be done together with the existing "fix chat photo uploads" item in section B. `Owner: ___`

## F. Backend cleanup
- [x] Delete the orphaned `dataIntegrityAudit` tRPC procedure. `Owner: Saboor`
- [ ] Decide and act on three more orphans in `server/contacts/router.ts`: `updateContact`, `fixMismatch`, `fixAllMismatches` — no UI calls any of them, and two are write endpoints, so this is a decision, not a tidy-up. `Owner: ___`

---

## Handoff to testing
- [ ] Flag each removal to Jason's team as it goes live, using their bug-report format. `Owner: ___`

---

**Net for M2:** the bulk is removals — each one needs a quick "is anything still wired
into this?" check before deleting, since the protocol build reaches into Programs,
Affiliate Partners and payment reminders. The chat work is the other half and shares one
backend across five UI surfaces, so changes there land everywhere at once. Farjad has the
protocol page overhaul.
