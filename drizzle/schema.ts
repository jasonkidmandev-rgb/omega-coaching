import { mysqlTable, mysqlSchema, AnyMySqlColumn, index, int, varchar, decimal, timestamp, mysqlEnum, text, json, foreignKey, datetime, date, bigint, tinyint, boolean, unique } from "drizzle-orm/mysql-core"
import { sql } from "drizzle-orm"

export const abandonedCheckouts = mysqlTable("abandoned_checkouts", {
	id: int().autoincrement().notNull(),
	userId: int(),
	email: varchar({ length: 320 }),
	clientName: varchar({ length: 255 }),
	planKey: varchar({ length: 100 }).notNull(),
	planName: varchar({ length: 255 }).notNull(),
	planPrice: decimal({ precision: 10, scale: 2 }).notNull(),
	startedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	completedAt: timestamp({ mode: 'string' }),
	recoveryEmailSentAt: timestamp({ mode: 'string' }),
	recoveryEmailOpenedAt: timestamp({ mode: 'string' }),
	recoveryEmailClickedAt: timestamp({ mode: 'string' }),
	recoveredAt: timestamp({ mode: 'string' }),
	sessionId: varchar({ length: 64 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("abandoned_checkout_email_idx").on(table.email),
	index("abandoned_checkout_user_idx").on(table.userId),
	index("abandoned_checkout_started_idx").on(table.startedAt),
	index("abandoned_checkout_completed_idx").on(table.completedAt),
	index("abandoned_checkout_session_idx").on(table.sessionId),
]);

export const adminInvitations = mysqlTable("admin_invitations", {
	id: int().autoincrement().notNull(),
	email: varchar({ length: 320 }).notNull(),
	name: varchar({ length: 255 }),
	role: mysqlEnum(['admin','manager','viewer','finance']).default('admin').notNull(),
	token: varchar({ length: 64 }).notNull(),
	invitedBy: int().notNull(),
	status: mysqlEnum(['pending','accepted','expired','revoked']).default('pending').notNull(),
	expiresAt: timestamp({ mode: 'string' }).notNull(),
	acceptedAt: timestamp({ mode: 'string' }),
	acceptedByUserId: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP'),
},
(table) => [
	index("admin_invitations_email_idx").on(table.email),
	index("admin_invitations_token_idx").on(table.token),
	index("admin_invitations_status_idx").on(table.status),
	index("token").on(table.token),
]);

export const adminSettings = mysqlTable("admin_settings", {
	id: int().autoincrement().notNull(),
	settingKey: varchar({ length: 100 }).notNull(),
	settingValue: text(),
	settingType: mysqlEnum(['string','number','boolean','json']).default('string').notNull(),
	description: text(),
	category: varchar({ length: 50 }).default('general').notNull(),
	updatedBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("admin_settings_key_idx").on(table.settingKey),
	index("admin_settings_category_idx").on(table.category),
	index("settingKey").on(table.settingKey),
]);

export const affiliateClicks = mysqlTable("affiliate_clicks", {
	id: int().autoincrement().notNull(),
	protocolItemId: int().notNull(),
	clientProtocolId: int(),
	clickedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	userAgent: text(),
	ipHash: varchar({ length: 64 }),
});

export const affiliatePartners = mysqlTable("affiliate_partners", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	url: text().notNull(),
	code: varchar({ length: 100 }),
	discountText: varchar({ length: 255 }),
	logoUrl: text(),
	category: mysqlEnum(['peptides','supplements','nootropics','tools','health','other']).default('other').notNull(),
	isFeatured: tinyint().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	testimonial: text(),
});

export const ageDisclaimers = mysqlTable("age_disclaimers", {
	id: int().autoincrement().notNull(),
	visitorId: varchar({ length: 64 }).notNull(),
	userId: int(),
	agreedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	ipAddress: varchar({ length: 45 }),
});

export const announcementHistory = mysqlTable("announcement_history", {
	id: int().autoincrement().notNull(),
	subject: varchar({ length: 500 }).notNull(),
	message: text().notNull(),
	recipientCount: int().notNull(),
	recipientWaiverIds: text(),
	filterCriteria: text(),
	status: mysqlEnum(['sent','scheduled','cancelled']).default('sent').notNull(),
	scheduledFor: timestamp({ mode: 'string' }),
	sentBy: int(),
	sentAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	trackingId: varchar({ length: 64 }),
	opens: int().default(0).notNull(),
	clicks: int().default(0).notNull(),
	recurrencePattern: mysqlEnum(['none','weekly','biweekly','monthly']).default('none').notNull(),
	recurrenceEndDate: timestamp({ mode: 'string' }),
	parentAnnouncementId: int(),
});

export const announcementTemplates = mysqlTable("announcement_templates", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	category: mysqlEnum(['product_updates','promotions','reminders','general']).default('general').notNull(),
	subject: varchar({ length: 500 }).notNull(),
	message: text().notNull(),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const appointmentPackages = mysqlTable("appointment_packages", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	appointmentTypeId: int().notNull(),
	sessionCount: int().notNull(),
	price: decimal({ precision: 10, scale: 2 }).notNull(),
	bonusSessions: int().default(0),
	validDays: int().default(365),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const appointmentTypes = mysqlTable("appointment_types", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	duration: int().default(30).notNull(),
	price: decimal({ precision: 10, scale: 2 }),
	color: varchar({ length: 7 }).default('#f97316'),
	isActive: tinyint().default(1).notNull(),
	allowOnlineBooking: tinyint().default(1).notNull(),
	requiresApproval: tinyint().default(0).notNull(),
	bufferBefore: int().default(0),
	bufferAfter: int().default(0),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	cancellationFee: decimal({ precision: 10, scale: 2 }),
	cancellationHours: int().default(24),
	sendFeedbackSurvey: tinyint().default(1).notNull(),
});

export const appointments = mysqlTable("appointments", {
	id: int().autoincrement().notNull(),
	coachId: int().notNull(),
	clientProtocolId: int(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 320 }).notNull(),
	clientPhone: varchar({ length: 50 }),
	appointmentTypeId: int().notNull(),
	startTime: timestamp({ mode: 'string' }).notNull(),
	endTime: timestamp({ mode: 'string' }).notNull(),
	status: mysqlEnum(['scheduled','confirmed','cancelled','completed','no_show']).default('scheduled').notNull(),
	notes: text(),
	clientNotes: text(),
	meetingLink: varchar({ length: 500 }),
	reminderSent: tinyint().default(0).notNull(),
	confirmationSent: tinyint().default(0).notNull(),
	cancellationReason: text(),
	cancelledAt: timestamp({ mode: 'string' }),
	cancelledBy: varchar({ length: 50 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	reminder24hSent: timestamp({ mode: 'string' }),
	reminder1hSent: timestamp({ mode: 'string' }),
	cancellationFeeCharged: decimal({ precision: 10, scale: 2 }),
	feedbackSurveySent: timestamp({ mode: 'string' }),
	feedbackRating: int(),
	feedbackComment: text(),
	sms24hSent: timestamp({ mode: 'string' }),
	sms1hSent: timestamp({ mode: 'string' }),
	smsOptIn: tinyint().default(1).notNull(),
	contactId: int(),
},
(table) => [
	index("appointment_coach_idx").on(table.coachId),
	index("appointment_contact_idx").on(table.contactId),
	index("appointment_client_idx").on(table.clientEmail),
	index("appointment_status_idx").on(table.status),
	index("appointment_start_idx").on(table.startTime),
]);

export const auditLogs = mysqlTable("audit_logs", {
	id: int().autoincrement().notNull(),
	adminId: int().notNull(),
	action: varchar({ length: 100 }).notNull(),
	targetUserId: int(),
	details: text(),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP'),
	userId: int(),
	userEmail: varchar({ length: 320 }),
	userRole: varchar({ length: 50 }),
	userIp: varchar({ length: 45 }),
	resourceType: varchar({ length: 100 }).default('unknown').notNull(),
	resourceId: varchar({ length: 100 }),
	resourceName: varchar({ length: 255 }),
	containsPhi: tinyint().default(0).notNull(),
	phiFields: text(),
	description: text(),
	metadata: text(),
	previousValue: text(),
	newValue: text(),
	requestId: varchar({ length: 100 }),
	sessionId: varchar({ length: 255 }),
	success: tinyint().default(1).notNull(),
	errorMessage: text(),
},
(table) => [
	index("audit_logs_admin_idx").on(table.adminId),
	index("audit_logs_target_user_idx").on(table.targetUserId),
	index("audit_logs_action_idx").on(table.action),
	index("audit_logs_created_at_idx").on(table.createdAt),
]);

export const automatedFollowUpSequences = mysqlTable("automated_follow_up_sequences", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	triggerEvent: mysqlEnum("trigger_event", ['checkin_submitted','checkin_missed','low_score','payment_received','protocol_sent','onboarding_complete','inventory_low','custom']).notNull(),
	triggerConditions: json("trigger_conditions"),
	isActive: tinyint("is_active").default(1).notNull(),
	createdBy: int("created_by"),
	createdAt: timestamp("created_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp("updated_at", { mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const automationEvents = mysqlTable("automation_events", {
	id: int().autoincrement().notNull(),
	eventType: varchar({ length: 100 }).notNull(),
	enrollmentId: int(),
	clientProtocolId: int(),
	clientProjectId: int(),
	clientId: int(),
	prospectId: int(),
	teamMemberId: int(),
	details: text(),
	status: mysqlEnum(['success','failed','skipped','completed']).default('success').notNull(),
	errorMessage: text(),
	triggeredBy: mysqlEnum(['payment','manual','cron','system']).default('payment').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("automation_events_type_idx").on(table.eventType),
	index("automation_events_enrollment_idx").on(table.enrollmentId),
	index("automation_events_created_at_idx").on(table.createdAt),
]);

export const availabilitySlots = mysqlTable("availability_slots", {
	id: int().autoincrement().notNull(),
	coachId: int().notNull(),
	dayOfWeek: int().notNull(),
	startTime: varchar({ length: 5 }).notNull(),
	endTime: varchar({ length: 5 }).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("availability_coach_idx").on(table.coachId),
]);

export const blockedSlots = mysqlTable("blocked_slots", {
	id: int().autoincrement().notNull(),
	coachId: int().notNull(),
	startDate: timestamp({ mode: 'string' }).notNull(),
	endDate: timestamp({ mode: 'string' }).notNull(),
	reason: varchar({ length: 255 }),
	isAllDay: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("blocked_coach_idx").on(table.coachId),
]);

export const calendlySettings = mysqlTable("calendly_settings", {
	id: int().autoincrement().notNull(),
	eventType: mysqlEnum(['discovery_session','reconstitution_training','week3_review','month2_session','final_review','general']).notNull(),
	calendlyUrl: text().notNull(),
	eventName: varchar({ length: 255 }).notNull(),
	durationMinutes: int().notNull(),
	description: text(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("calendly_event_type_idx").on(table.eventType),
]);

export const categories = mysqlTable("categories", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	isDiscountable: tinyint().default(1).notNull(),
	displayName: varchar({ length: 255 }),
	iconUrl: varchar({ length: 500 }),
	isActive: tinyint().default(1).notNull(),
});

export const checkinCoachResponses = mysqlTable("checkin_coach_responses", {
	id: int().autoincrement().notNull(),
	checkinId: int().notNull(),
	coachId: int().notNull(),
	responseType: mysqlEnum(['text','voice','video']).notNull(),
	textContent: text(),
	mediaUrl: text(),
	mediaDuration: int(),
	clientNotifiedAt: timestamp({ mode: 'string' }),
	clientViewedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("checkin_coach_responses_checkin_idx").on(table.checkinId),
]);

export const checkinNotificationLogs = mysqlTable("checkin_notification_logs", {
	id: int().autoincrement().notNull(),
	checkinId: int(),
	clientProtocolId: int().notNull(),
	notificationType: varchar({ length: 50 }).notNull(),
	recipientType: mysqlEnum(['client','coach']).notNull(),
	recipientEmail: varchar({ length: 320 }),
	subject: varchar({ length: 500 }),
	status: mysqlEnum(['sent','failed','pending']).default('pending').notNull(),
	errorMessage: text(),
	sentAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("checkin_notification_logs_checkin_idx").on(table.checkinId),
	index("checkin_notification_logs_client_protocol_idx").on(table.clientProtocolId),
	index("checkin_notification_logs_sent_at_idx").on(table.sentAt),
]);

export const checkinNotificationTemplates = mysqlTable("checkin_notification_templates", {
	id: int().autoincrement().notNull(),
	templateType: varchar({ length: 50 }).notNull(),
	subject: varchar({ length: 500 }).notNull(),
	bodyHtml: text().notNull(),
	bodyText: text(),
	isActive: tinyint().default(1).notNull(),
	availablePlaceholders: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	category: varchar({ length: 50 }).default('checkin').notNull(),
	name: varchar({ length: 200 }).default('').notNull(),
	description: text(),
	isCustomized: tinyint().default(0).notNull(),
},
(table) => [
	index("templateType").on(table.templateType),
]);

export const checkinResponses = mysqlTable("checkin_responses", {
	id: int().autoincrement().notNull(),
	checkinId: int().notNull(),
	questionId: varchar({ length: 50 }).notNull(),
	questionText: text().notNull(),
	questionType: varchar({ length: 20 }).notNull(),
	scaleValue: int(),
	textValue: text(),
	booleanValue: tinyint(),
	selectValue: varchar({ length: 255 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("checkin_responses_checkin_idx").on(table.checkinId),
]);

export const checkinScheduleAuditLog = mysqlTable("checkin_schedule_audit_log", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	changedBy: int().references(() => users.id),
	changedByName: varchar({ length: 255 }),
	changedByEmail: varchar({ length: 320 }),
	action: mysqlEnum(['created','updated','enabled','disabled','paused','resumed','bulk_updated','bulk_enabled','bulk_disabled']).notNull(),
	previousFrequency: varchar({ length: 20 }),
	previousDayOfWeek: int(),
	previousTimeOfDay: varchar({ length: 10 }),
	previousTimezone: varchar({ length: 50 }),
	previousNextScheduledAt: timestamp({ mode: 'string' }),
	newFrequency: varchar({ length: 20 }),
	newDayOfWeek: int(),
	newTimeOfDay: varchar({ length: 10 }),
	newTimezone: varchar({ length: 50 }),
	newNextScheduledAt: timestamp({ mode: 'string' }),
	notes: text(),
	ipAddress: varchar({ length: 45 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("checkin_audit_protocol_idx").on(table.clientProtocolId),
	index("checkin_audit_changed_by_idx").on(table.changedBy),
	index("checkin_audit_action_idx").on(table.action),
	index("checkin_audit_created_idx").on(table.createdAt),
]);

export const checkinSchedules = mysqlTable("checkin_schedules", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	templateId: int().notNull(),
	isEnabled: tinyint().default(0).notNull(),
	frequency: mysqlEnum(['weekly','biweekly','monthly']).default('weekly').notNull(),
	dayOfWeek: int().default(4).notNull(),
	timeOfDay: varchar({ length: 5 }).default('10:00').notNull(),
	timezone: varchar({ length: 50 }).default('America/Denver').notNull(),
	lastSentAt: timestamp({ mode: 'string' }),
	nextScheduledAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	isPaused: tinyint("is_paused").default(0).notNull(),
	skipUntil: timestamp("skip_until", { mode: 'string' }),
	pausedReason: varchar("paused_reason", { length: 255 }),
	currentStreak: int().default(0).notNull(),
	longestStreak: int().default(0).notNull(),
	totalResponses: int().default(0).notNull(),
	totalSent: int().default(0).notNull(),
	lastResponseAt: timestamp({ mode: 'string' }),
},
(table) => [
	index("checkin_schedules_client_protocol_idx").on(table.clientProtocolId),
	index("checkin_schedules_enabled_idx").on(table.isEnabled),
]);

export const checkinTemplates = mysqlTable("checkin_templates", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	isDefault: tinyint().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	questions: json().notNull(),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const checkins = mysqlTable("checkins", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	templateId: int().notNull(),
	scheduleId: int(),
	status: mysqlEnum(['pending','submitted','reviewed','incomplete']).default('pending').notNull(),
	overallScore: int(),
	hasLowScore: tinyint().default(0).notNull(),
	lowestScore: int(),
	sentAt: timestamp({ mode: 'string' }),
	reminder1SentAt: timestamp({ mode: 'string' }),
	reminder2SentAt: timestamp({ mode: 'string' }),
	submittedAt: timestamp({ mode: 'string' }),
	reviewedAt: timestamp({ mode: 'string' }),
	reviewedBy: int(),
	dueAt: timestamp({ mode: 'string' }),
	weekNumber: int(),
	periodStart: timestamp({ mode: 'string' }),
	periodEnd: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	alertProcessedAt: timestamp("alert_processed_at", { mode: 'string' }),
},
(table) => [
	index("checkins_client_protocol_idx").on(table.clientProtocolId),
	index("checkins_status_idx").on(table.status),
	index("checkins_submitted_at_idx").on(table.submittedAt),
	index("checkins_has_low_score_idx").on(table.hasLowScore),
]);

export const clientAchievements = mysqlTable("client_achievements", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	achievementType: varchar({ length: 50 }).notNull(),
	title: varchar({ length: 100 }).notNull(),
	description: text(),
	iconUrl: varchar({ length: 500 }),
	earnedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	streakCount: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("client_achievements_client_protocol_idx").on(table.clientProtocolId),
	index("client_achievements_type_idx").on(table.achievementType),
]);

export const clientActivityScores = mysqlTable("client_activity_scores", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int("client_protocol_id").notNull(),
	checkinScore: int("checkin_score").default(0).notNull(),
	documentScore: int("document_score").default(0).notNull(),
	purchaseScore: int("purchase_score").default(0).notNull(),
	engagementScore: int("engagement_score").default(0).notNull(),
	totalScore: int("total_score").default(0).notNull(),
	lastActivityAt: timestamp("last_activity_at", { mode: 'string' }),
	checkinsCompleted: int("checkins_completed").default(0).notNull(),
	checkinsMissed: int("checkins_missed").default(0).notNull(),
	documentsUploaded: int("documents_uploaded").default(0).notNull(),
	purchasesMade: int("purchases_made").default(0).notNull(),
	commentsMade: int("comments_made").default(0).notNull(),
	calculatedAt: timestamp("calculated_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	createdAt: timestamp("created_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp("updated_at", { mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("client_protocol_id").on(table.clientProtocolId),
	index("total_score").on(table.totalScore),
	index("last_activity_at").on(table.lastActivityAt),
	index("client_protocol_id_2").on(table.clientProtocolId),
]);

export const clientInventory = mysqlTable("client_inventory", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	protocolItemId: int(),
	itemName: varchar({ length: 255 }).notNull(),
	itemCategory: varchar({ length: 100 }),
	status: mysqlEnum(['full','half','running_low','out']).default('full').notNull(),
	isCustom: tinyint().default(0).notNull(),
	addedBy: mysqlEnum(['system','coach','client']).default('system').notNull(),
	expectedQuantity: int(),
	notes: text(),
	lastReorderAlertAt: timestamp({ mode: 'string' }),
	reorderTaskCreated: tinyint().default(0).notNull(),
	inventoryItemId: int(),
	lastUpdatedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("client_inventory_client_protocol_idx").on(table.clientProtocolId),
	index("client_inventory_status_idx").on(table.status),
	index("client_inventory_protocol_item_idx").on(table.protocolItemId),
]);

export const clientMetrics = mysqlTable("client_metrics", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	userId: int(),
	metricType: mysqlEnum(['weight','body_fat','lean_mass','waist','chest','arms','custom']).notNull(),
	customMetricName: varchar({ length: 100 }),
	value: decimal({ precision: 10, scale: 2 }).notNull(),
	unit: varchar({ length: 20 }).default('lbs').notNull(),
	notes: text(),
	recordedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	source: mysqlEnum(['manual','checkin','body_scan']).default('manual').notNull(),
	sourceId: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("client_metrics_client_protocol_idx").on(table.clientProtocolId),
	index("client_metrics_metric_type_idx").on(table.metricType),
	index("client_metrics_recorded_at_idx").on(table.recordedAt),
]);

export const clientNotificationHistory = mysqlTable("client_notification_history", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int(),
	userId: int(),
	recipientEmail: varchar({ length: 320 }).notNull(),
	recipientName: varchar({ length: 255 }),
	category: mysqlEnum(['checkin','protocol','payment','shipping','inventory','document','welcome','announcement','digest','notification','message','other']).notNull(),
	notificationType: varchar({ length: 100 }).notNull(),
	subject: varchar({ length: 500 }).notNull(),
	previewText: text(),
	status: mysqlEnum(['sent','failed','pending','bounced']).default('pending').notNull(),
	errorMessage: text(),
	relatedEntityType: varchar({ length: 50 }),
	relatedEntityId: int(),
	scheduledAt: timestamp({ mode: 'string' }),
	sentAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	triggeredBy: mysqlEnum(['system','cron','admin','webhook']).default('system').notNull(),
	triggeredByUserId: int(),
	trackingId: varchar({ length: 64 }),
	openedAt: timestamp({ mode: 'string' }),
	openCount: int().default(0).notNull(),
	clickedAt: timestamp({ mode: 'string' }),
	clickCount: int().default(0).notNull(),
},
(table) => [
	index("client_notification_history_client_protocol_idx").on(table.clientProtocolId),
	index("client_notification_history_user_idx").on(table.userId),
	index("client_notification_history_category_idx").on(table.category),
	index("client_notification_history_sent_at_idx").on(table.sentAt),
	index("client_notification_history_status_idx").on(table.status),
	index("client_notification_history_tracking_idx").on(table.trackingId),
]);

export const clientNotificationPreferences = mysqlTable("client_notification_preferences", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	paymentRemindersEnabled: tinyint().default(1).notNull(),
	reminderDays: varchar({ length: 50 }).default('3,7,14').notNull(),
	lastReminderSentAt: timestamp({ mode: 'string' }),
	reminderCount: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("client_notification_prefs_protocol_idx").on(table.clientProtocolId),
	index("clientProtocolId").on(table.clientProtocolId),
]);

export const clientOnboardingChecklist = mysqlTable("client_onboarding_checklist", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int("client_protocol_id").notNull(),
	stepKey: varchar("step_key", { length: 50 }).notNull(),
	stepName: varchar("step_name", { length: 100 }).notNull(),
	stepDescription: text("step_description"),
	isCompleted: tinyint("is_completed").default(0).notNull(),
	completedAt: timestamp("completed_at", { mode: 'string' }),
	sortOrder: int("sort_order").default(0).notNull(),
	createdAt: timestamp("created_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp("updated_at", { mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("idx_client_protocol").on(table.clientProtocolId),
	index("unique_client_step").on(table.clientProtocolId, table.stepKey),
]);

export const clientPackages = mysqlTable("client_packages", {
	id: int().autoincrement().notNull(),
	clientEmail: varchar({ length: 320 }).notNull(),
	packageId: int().notNull(),
	sessionsRemaining: int().notNull(),
	sessionsUsed: int().default(0).notNull(),
	purchasedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	expiresAt: timestamp({ mode: 'string' }),
	status: mysqlEnum(['active','expired','exhausted']).default('active').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	contactId: int(),
},
(table) => [
	index("client_package_contact_idx").on(table.contactId),
	index("client_package_client_idx").on(table.clientEmail),
	index("client_package_status_idx").on(table.status),
]);

export const clientProjects = mysqlTable("client_projects", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 320 }),
	workflowTemplateId: int(),
	currentLifecycleStageId: int(),
	status: mysqlEnum(['active','on_hold','completed','cancelled']).default('active').notNull(),
	priority: mysqlEnum(['low','normal','high','urgent']).default('normal').notNull(),
	startDate: timestamp({ mode: 'string' }),
	targetEndDate: timestamp({ mode: 'string' }),
	actualEndDate: timestamp({ mode: 'string' }),
	assignedTeamMemberId: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	contactId: int(),
},
(table) => [
	index("client_project_contact_idx").on(table.contactId),
]);

export const clientProtocolItems = mysqlTable("client_protocol_items", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	protocolItemId: int().notNull(),
	quantity: int().default(1).notNull(),
	isIncluded: tinyint().default(1).notNull(),
	isRecommended: tinyint().default(1).notNull(),
	customSchedule: text(),
	customDuration: varchar({ length: 255 }),
	customPrice: decimal({ precision: 10, scale: 2 }),
	customNotes: text(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	customCategoryName: varchar({ length: 255 }),
	fulfillmentSource: mysqlEnum(['coach','client']).default('coach').notNull(),
	snapshotName: varchar({ length: 500 }),
	customPurpose: text(),
},
(table) => [
	index("client_protocol_items_protocol_idx").on(table.clientProtocolId),
	index("client_protocol_items_item_idx").on(table.protocolItemId),
]);

export const clientProtocolRequirements = mysqlTable("client_protocol_requirements", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	requirementId: int(),
	customText: text(),
	isIncluded: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
});

export const clientProtocols = mysqlTable("client_protocols", {
	id: int().autoincrement().notNull(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 320 }),
	clientPhone: varchar({ length: 50 }),
	accessToken: varchar({ length: 64 }).notNull(),
	templateId: int(),
	durationMonths: int().default(3).notNull(),
	status: mysqlEnum(['draft','pending_approval','approved','active','completed']).default('draft').notNull(),
	approvedAt: timestamp({ mode: 'string' }),
	discountPercent: decimal({ precision: 5, scale: 2 }).default('0'),
	coachingPackage: varchar({ length: 255 }),
	coachingPrice: decimal({ precision: 10, scale: 2 }).default('0'),
	paymentMethod: mysqlEnum(['venmo','cc','other','paypal']).default('venmo'),
	venmoHandle: varchar({ length: 100 }),
	customRequirements: text(),
	notes: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	firstViewedAt: timestamp({ mode: 'string' }),
	archivedAt: timestamp({ mode: 'string' }),
	deletedAt: timestamp({ mode: 'string' }),
	programId: int(),
	currentPhaseId: int(),
	phaseStartDate: timestamp({ mode: 'string' }),
	programStartDate: timestamp({ mode: 'string' }),
	coachNotes: text(),
	sentAt: timestamp({ mode: 'string' }),
	shippingName: varchar({ length: 255 }),
	shippingStreet: varchar({ length: 500 }),
	shippingCity: varchar({ length: 255 }),
	shippingState: varchar({ length: 100 }),
	shippingZip: varchar({ length: 20 }),
	shippingCountry: varchar({ length: 100 }),
	shippingPhone: varchar({ length: 50 }),
	onboardingCompletedAt: timestamp({ mode: 'string' }),
	onboardingPath: varchar({ length: 50 }),
	onboardingSelection: varchar({ length: 100 }),
	lastFollowUpSentAt: timestamp({ mode: 'string' }),
	followUpCount: int().default(0).notNull(),
	internalNotes: text(),
	tags: text(),
	paymentStatus: mysqlEnum(['pending','paid','failed','refunded']).default('pending'),
	paymentReceivedAt: timestamp({ mode: 'string' }),
	showAffiliateLinks: tinyint().default(1).notNull(),
	inviteSentAt: timestamp({ mode: 'string' }),
	paymentReminderOptOut: tinyint().default(0).notNull(),
	clientId: int(),
	version: int().default(1).notNull(),
	versionName: varchar({ length: 100 }),
	isActiveVersion: tinyint().default(1).notNull(),
	previousVersionId: int(),
	completedAt: timestamp({ mode: 'string' }),
	versionNotes: text(),
	clientVisibility: mysqlEnum(['hidden','option','active','archived']).default('active').notNull(),
	engagementLevel: mysqlEnum(['full_coaching','self_guided_checkins','protocol_only']).default('protocol_only').notNull(),
	lastViewedAt: timestamp({ mode: 'string' }),
	viewCount: int().default(0).notNull(),
	renewedFromId: int(),
	startDate: timestamp({ mode: 'string' }),
	endDate: timestamp({ mode: 'string' }),
	inventoryDeductedAt: timestamp({ mode: 'string' }),
	contactId: int(),
},
(table) => [
	index("client_protocols_accessToken_unique").on(table.accessToken),
	index("client_protocols_contact_idx").on(table.contactId),
	index("client_protocols_email_idx").on(table.clientEmail),
	index("client_protocols_status_idx").on(table.status),
	index("client_protocols_template_idx").on(table.templateId),
	index("client_protocols_program_idx").on(table.programId),
	index("client_protocols_client_id_idx").on(table.clientId),
	index("client_protocols_client_active_idx").on(table.clientId, table.isActiveVersion),
]);

export const clients = mysqlTable("clients", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	email: varchar({ length: 320 }),
	phone: varchar({ length: 50 }),
	shippingName: varchar({ length: 255 }),
	shippingStreet: varchar({ length: 500 }),
	shippingCity: varchar({ length: 255 }),
	shippingState: varchar({ length: 100 }),
	shippingZip: varchar({ length: 20 }),
	shippingCountry: varchar({ length: 100 }).default('USA'),
	shippingPhone: varchar({ length: 50 }),
	ghlContactId: varchar({ length: 100 }),
	referralSource: varchar({ length: 255 }),
	notes: text(),
	tags: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	archivedAt: timestamp({ mode: 'string' }),
	deletedAt: timestamp({ mode: 'string' }),
	isActiveInProjects: tinyint().default(0).notNull(),
	clientProjectId: int(),
},
(table) => [
	index("clients_email_idx").on(table.email),
	index("clients_name_idx").on(table.name),
]);

export const cloneHistory = mysqlTable("clone_history", {
	id: int().autoincrement().notNull(),
	sourceProtocolId: int(),
	sourceProtocolName: varchar({ length: 255 }).notNull(),
	targetProtocolId: int().notNull(),
	targetProtocolName: varchar({ length: 255 }).notNull(),
	cloneType: mysqlEnum(['new_client','existing_client','bulk','from_template']).notNull(),
	itemsCloned: int().default(0).notNull(),
	clonedBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const coachProtocolPresets = mysqlTable("coach_protocol_presets", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	category: varchar({ length: 100 }),
	isDefault: tinyint().default(0).notNull(),
	createdBy: int(),
	itemConfig: json().notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	isSystemPreset: tinyint().default(0).notNull(),
	items: json(),
},
(table) => [
	index("idx_category").on(table.category),
	index("idx_created_by").on(table.createdBy),
]);

export const coachingPackages = mysqlTable("coaching_packages", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	price: decimal({ precision: 10, scale: 2 }).default('0'),
	durationDays: int().default(90),
	features: text(),
	linkUrl: text(),
	isActive: tinyint().default(1).notNull(),
	isComingSoon: tinyint().default(0).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const coachingSessionNotes = mysqlTable("coaching_session_notes", {
	id: int().autoincrement().notNull(),
	enrollmentId: int("enrollment_id").notNull(),
	userId: int("user_id"),
	sessionDate: datetime("session_date", { mode: 'string'}).notNull(),
	sessionType: mysqlEnum("session_type", ['discovery','check_in','training','reconstitution','ad_hoc','follow_up']).default('ad_hoc').notNull(),
	content: text().notNull(),
	coachId: int("coach_id"),
	coachName: varchar("coach_name", { length: 255 }),
	isPinned: tinyint("is_pinned").default(0),
	createdAt: datetime("created_at", { mode: 'string'}).default('CURRENT_TIMESTAMP'),
	updatedAt: datetime("updated_at", { mode: 'string'}).default(sql`(CURRENT_TIMESTAMP)`),
},
(table) => [
	index("idx_enrollment_id").on(table.enrollmentId),
	index("idx_user_id").on(table.userId),
	index("idx_session_date").on(table.sessionDate),
	index("idx_session_type").on(table.sessionType),
]);

export const contacts = mysqlTable("contacts", {
	id: int().autoincrement().notNull(),
	firstName: varchar("first_name", { length: 255 }),
	lastName: varchar("last_name", { length: 255 }),
	fullName: varchar("full_name", { length: 500 }),
	email: varchar({ length: 320 }),
	phone: varchar({ length: 50 }),
	secondaryEmail: varchar("secondary_email", { length: 320 }),
	secondaryPhone: varchar("secondary_phone", { length: 50 }),
	preferredContactMethod: mysqlEnum("preferred_contact_method", ['email', 'phone', 'sms']).default('email'),
	lifecycleStage: mysqlEnum("lifecycle_stage", ['lead', 'prospect', 'enrolled', 'active_client', 'past_client', 'store_customer']).default('lead').notNull(),
	source: varchar({ length: 100 }),
	notes: text(),
	createdAt: timestamp("created_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp("updated_at", { mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("contacts_email_unique").on(table.email),
	index("contacts_phone_idx").on(table.phone),
	index("contacts_full_name_idx").on(table.fullName),
	index("contacts_lifecycle_idx").on(table.lifecycleStage),
	index("contacts_created_idx").on(table.createdAt),
]);

export const consultationNotes = mysqlTable("consultation_notes", {
	id: int().autoincrement().notNull(),
	prospectId: int(),
	enrollmentId: int(),
	clientId: int(),
	authorId: int().notNull(),
	consultType: mysqlEnum(['quick_hit_20min','strategy_session','discovery_call','follow_up','other']).default('quick_hit_20min').notNull(),
	notes: text().notNull(),
	recommendations: text(),
	nextSteps: text(),
	suggestedTier: varchar({ length: 50 }),
	suggestedProgram: varchar({ length: 255 }),
	consultDate: timestamp({ mode: 'string' }).notNull(),
	reminderSentAt: timestamp({ mode: 'string' }),
	noteEnteredAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("consultation_notes_prospect_idx").on(table.prospectId),
	index("consultation_notes_enrollment_idx").on(table.enrollmentId),
	index("consultation_notes_author_idx").on(table.authorId),
	index("consultation_notes_consult_date_idx").on(table.consultDate),
]);

export const couponUsage = mysqlTable("coupon_usage", {
	id: int().autoincrement().notNull(),
	couponId: int().notNull(),
	clientProtocolId: int().notNull(),
	discountApplied: decimal({ precision: 5, scale: 2 }).notNull(),
	usedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const coupons = mysqlTable("coupons", {
	id: int().autoincrement().notNull(),
	code: varchar({ length: 50 }).notNull(),
	discountPercent: decimal({ precision: 5, scale: 2 }).notNull(),
	usageType: mysqlEnum(['one_time','unlimited']).default('unlimited').notNull(),
	scope: mysqlEnum(['universal','client_specific']).default('universal').notNull(),
	clientProtocolId: int(),
	expiresAt: timestamp({ mode: 'string' }),
	maxUses: int(),
	currentUses: int().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	isFlagged: tinyint().default(0).notNull(),
	notes: text(),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	category: varchar({ length: 100 }),
	deactivationReason: varchar({ length: 100 }),
},
(table) => [
	index("coupons_code_unique").on(table.code),
]);

export const cronRuns = mysqlTable("cron_runs", {
	id: int().autoincrement().notNull(),
	jobName: varchar({ length: 100 }).notNull(),
	status: mysqlEnum(['success','error','partial']).notNull(),
	startedAt: timestamp({ mode: 'string' }).notNull(),
	completedAt: timestamp({ mode: 'string' }),
	durationMs: int(),
	itemsProcessed: int().default(0),
	itemsSucceeded: int().default(0),
	itemsFailed: int().default(0),
	errorMessage: text(),
	details: text(),
	triggeredBy: mysqlEnum(['cron','manual','startup']).default('cron').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("cron_runs_job_name_idx").on(table.jobName),
	index("cron_runs_started_at_idx").on(table.startedAt),
	index("cron_runs_job_status_idx").on(table.jobName, table.status),
]);

export const customOrderItems = mysqlTable("custom_order_items", {
	id: int().autoincrement().notNull(),
	customOrderId: int().notNull(),
	inventoryItemId: int(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	quantity: int().notNull(),
	pricePerUnit: decimal({ precision: 10, scale: 2 }).notNull(),
	originalPrice: decimal({ precision: 10, scale: 2 }),
	itemType: mysqlEnum(['product','service','shipping','custom']).default('product').notNull(),
	isDiscountable: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("custom_order_item_order_idx").on(table.customOrderId),
	index("custom_order_item_inventory_idx").on(table.inventoryItemId),
]);

export const customOrderVenmoPayments = mysqlTable("custom_order_venmo_payments", {
	id: int().autoincrement().notNull(),
	customOrderId: int().notNull(),
	amount: decimal({ precision: 10, scale: 2 }).notNull(),
	venmoUsername: varchar({ length: 100 }),
	venmoTransactionNote: text(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 320 }),
	status: mysqlEnum(['pending','confirmed','rejected']).default('pending').notNull(),
	verifiedBy: int(),
	verifiedAt: timestamp({ mode: 'string' }),
	verificationNotes: text(),
	grossAmount: decimal({ precision: 10, scale: 2 }),
	feeAmount: decimal({ precision: 10, scale: 2 }),
	netAmount: decimal({ precision: 10, scale: 2 }),
	submittedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("custom_venmo_order_idx").on(table.customOrderId),
	index("custom_venmo_status_idx").on(table.status),
]);

export const customOrders = mysqlTable("custom_orders", {
	id: int().autoincrement().notNull(),
	orderNumber: varchar({ length: 50 }).notNull(),
	userId: int(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 320 }).notNull(),
	clientPhone: varchar({ length: 50 }),
	paypalOrderId: varchar({ length: 255 }),
	venmoTransactionId: varchar({ length: 255 }),
	paymentMethod: mysqlEnum(['paypal','venmo','stripe','manual']).notNull(),
	subtotal: decimal({ precision: 10, scale: 2 }).notNull(),
	discountAmount: decimal({ precision: 10, scale: 2 }).default('0').notNull(),
	shippingFee: decimal({ precision: 10, scale: 2 }).default('0').notNull(),
	total: decimal({ precision: 10, scale: 2 }).notNull(),
	status: mysqlEnum(['draft','pending_payment','pending_venmo','paid','processing','shipped','delivered','cancelled','refunded']).default('draft').notNull(),
	shippingName: varchar({ length: 255 }),
	shippingStreet: varchar({ length: 500 }),
	shippingCity: varchar({ length: 255 }),
	shippingState: varchar({ length: 100 }),
	shippingZip: varchar({ length: 20 }),
	shippingCountry: varchar({ length: 100 }),
	shippingPhone: varchar({ length: 50 }),
	shippingMethod: mysqlEnum(['standard','expedited','overnight','pickup']).default('standard'),
	trackingNumber: varchar({ length: 255 }),
	trackingCarrier: varchar({ length: 100 }),
	adminNotes: text(),
	createdBy: int().notNull(),
	createdByName: varchar({ length: 255 }),
	paypalFeeAmount: decimal({ precision: 10, scale: 2 }),
	paypalNetAmount: decimal({ precision: 10, scale: 2 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	paidAt: timestamp({ mode: 'string' }),
	shippedAt: timestamp({ mode: 'string' }),
	deliveredAt: timestamp({ mode: 'string' }),
	cancelledAt: timestamp({ mode: 'string' }),
	contactId: int(),
},
(table) => [
	index("custom_order_contact_idx").on(table.contactId),
	index("custom_order_user_idx").on(table.userId),
	index("custom_order_status_idx").on(table.status),
	index("custom_order_number_idx").on(table.orderNumber),
	index("custom_order_paypal_idx").on(table.paypalOrderId),
	index("custom_order_created_at_idx").on(table.createdAt),
]);

export const dashboardPreferences = mysqlTable("dashboard_preferences", {
	id: int().autoincrement().notNull(),
	userId: int().notNull().references(() => users.id),
	widgetVisibility: json(),
	widgetOrder: json(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("dashboard_preferences_user_idx").on(table.userId),
]);

export const dataAccessRequests = mysqlTable("data_access_requests", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	requestType: varchar({ length: 50 }).notNull(),
	status: varchar({ length: 50 }).default('pending').notNull(),
	requestedData: text(),
	processedAt: timestamp({ mode: 'string' }),
	processedBy: int(),
	notes: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("data_access_requests_user_idx").on(table.userId),
	index("data_access_requests_status_idx").on(table.status),
]);

export const documentFolders = mysqlTable("document_folders", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	parentId: int(),
	isSystem: tinyint().default(0).notNull(),
	systemType: varchar({ length: 50 }),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("document_folders_client_protocol_idx").on(table.clientProtocolId),
	index("document_folders_parent_idx").on(table.parentId),
]);

export const documentRequests = mysqlTable("document_requests", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	requestedBy: int().notNull(),
	title: varchar({ length: 255 }).notNull(),
	description: text(),
	targetFolderId: int(),
	status: mysqlEnum(['pending','completed','cancelled']).default('pending').notNull(),
	dueDate: timestamp({ mode: 'string' }),
	completedAt: timestamp({ mode: 'string' }),
	completedDocumentId: int(),
	notifiedAt: timestamp({ mode: 'string' }),
	reminderSentAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("document_requests_client_protocol_idx").on(table.clientProtocolId),
	index("document_requests_status_idx").on(table.status),
]);

export const documents = mysqlTable("documents", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	folderId: int().notNull(),
	name: varchar({ length: 500 }).notNull(),
	description: text(),
	s3Key: varchar({ length: 500 }).notNull(),
	s3Url: text().notNull(),
	fileSize: int(),
	mimeType: varchar({ length: 100 }),
	visibility: mysqlEnum(['shared','coach_only']).default('shared').notNull(),
	uploadedBy: mysqlEnum(['coach','client','system']).notNull(),
	uploadedByUserId: int(),
	version: int().default(1).notNull(),
	previousVersionId: int(),
	expiresAt: timestamp({ mode: 'string' }),
	expirationNotifiedAt: timestamp({ mode: 'string' }),
	autoFiledFrom: varchar({ length: 50 }),
	sourceId: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	deletedAt: timestamp({ mode: 'string' }),
},
(table) => [
	index("documents_client_protocol_idx").on(table.clientProtocolId),
	index("documents_folder_idx").on(table.folderId),
	index("documents_visibility_idx").on(table.visibility),
]);

export const emailBrandingSettings = mysqlTable("email_branding_settings", {
	id: int().autoincrement().notNull(),
	logoUrl: text(),
	primaryColor: varchar({ length: 7 }).default('#ea580c'),
	secondaryColor: varchar({ length: 7 }).default('#1e40af'),
	companyName: varchar({ length: 255 }).default('Omega Longevity'),
	tagline: varchar({ length: 255 }).default('Elite Level Health Optimization'),
	footerText: text(),
	socialLinks: text(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const emailEngagementEvents = mysqlTable("email_engagement_events", {
	id: int().autoincrement().notNull(),
	notificationHistoryId: int(),
	trackingId: varchar({ length: 64 }).notNull(),
	eventType: mysqlEnum(['open','click']).notNull(),
	linkUrl: text(),
	linkName: varchar({ length: 255 }),
	userAgent: text(),
	ipAddress: varchar({ length: 45 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("email_engagement_tracking_idx").on(table.trackingId),
	index("email_engagement_notification_idx").on(table.notificationHistoryId),
	index("email_engagement_event_type_idx").on(table.eventType),
	index("trackingId").on(table.trackingId),
]);

export const emailEvents = mysqlTable("email_events", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	eventType: mysqlEnum(['sent','opened','clicked','bounced']).notNull(),
	emailType: mysqlEnum(['protocol_link','protocol_pdf','reminder','notification']).notNull(),
	recipientEmail: varchar({ length: 320 }).notNull(),
	trackingToken: varchar({ length: 64 }).notNull(),
	metadata: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	linkUrl: text(),
	linkName: varchar({ length: 255 }),
},
(table) => [
	index("email_events_trackingToken_unique").on(table.trackingToken),
	index("email_events_protocol_idx").on(table.clientProtocolId),
	index("email_events_type_idx").on(table.eventType),
	index("email_events_recipient_idx").on(table.recipientEmail),
]);

export const emailReportSettings = mysqlTable("email_report_settings", {
	id: int().autoincrement().notNull(),
	reportType: varchar({ length: 50 }).notNull(),
	frequency: varchar({ length: 20 }).notNull(),
	isEnabled: tinyint().default(1),
	recipients: json(),
	dayOfWeek: int(),
	dayOfMonth: int(),
	hourOfDay: int().default(9),
	lastSentAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const emailTemplateCustomizations = mysqlTable("email_template_customizations", {
	id: int().autoincrement().notNull(),
	templateKey: varchar({ length: 100 }).notNull(),
	subject: text(),
	bodyHtml: text(),
	bodyText: text(),
	variables: json(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	updatedBy: int().references(() => users.id),
},
(table) => [
	index("templateKey").on(table.templateKey),
]);

export const emailTemplateVersions = mysqlTable("email_template_versions", {
	id: int().autoincrement().notNull(),
	templateKey: varchar({ length: 100 }).notNull(),
	version: int().notNull(),
	subject: text(),
	bodyHtml: text(),
	bodyText: text(),
	versionName: varchar({ length: 255 }),
	versionNotes: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	createdBy: int(),
},
(table) => [
	index("email_template_versions_key_idx").on(table.templateKey),
	index("email_template_versions_version_idx").on(table.templateKey, table.version),
]);

export const emailTracking = mysqlTable("email_tracking", {
	id: int().autoincrement().notNull(),
	trackingId: varchar({ length: 64 }).notNull(),
	emailType: mysqlEnum(['payment_reminder','protocol_sent','welcome','profile_reminder','checkin','announcement','session_reminder','transformation_milestone','transformation_welcome','transformation_payment','transformation_admin','resend_welcome','new_message_to_client','new_message_to_admin','other']).notNull(),
	recipientEmail: varchar({ length: 320 }).notNull(),
	recipientName: varchar({ length: 255 }),
	subject: varchar({ length: 500 }),
	clientProtocolId: int(),
	enrollmentId: int(),
	userId: int(),
	sentAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	openedAt: timestamp({ mode: 'string' }),
	openCount: int().default(0).notNull(),
	clickedAt: timestamp({ mode: 'string' }),
	clickCount: int().default(0).notNull(),
	metadata: text(),
	lastClickedLink: text(),
	userAgent: text(),
	ipAddress: varchar({ length: 45 }),
	updatedAt: timestamp({ mode: 'string' }).onUpdateNow(),
},
(table) => [
	index("email_tracking_tracking_id_idx").on(table.trackingId),
	index("email_tracking_recipient_idx").on(table.recipientEmail),
	index("email_tracking_email_type_idx").on(table.emailType),
	index("email_tracking_sent_at_idx").on(table.sentAt),
	index("email_tracking_client_protocol_idx").on(table.clientProtocolId),
	index("trackingId").on(table.trackingId),
	index("email_tracking_enrollment_idx").on(table.enrollmentId),
]);

export const emailTrackingClicks = mysqlTable("email_tracking_clicks", {
	id: int().autoincrement().notNull(),
	trackingId: varchar({ length: 64 }).notNull(),
	linkUrl: text().notNull(),
	linkLabel: varchar({ length: 255 }),
	clickedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	userAgent: text(),
	ipAddress: varchar({ length: 45 }),
},
(table) => [
	index("email_clicks_tracking_idx").on(table.trackingId),
	index("email_clicks_clicked_at_idx").on(table.clickedAt),
]);

export const embedTokens = mysqlTable("embed_tokens", {
	id: int().autoincrement().notNull(),
	token: varchar({ length: 64 }).notNull(),
	userId: int(),
	email: varchar({ length: 255 }),
	name: varchar({ length: 255 }),
	source: varchar({ length: 50 }).default('ghl'),
	permissions: json(),
	expiresAt: timestamp({ mode: 'string' }),
	lastUsedAt: timestamp({ mode: 'string' }),
	usageCount: int().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("embed_tokens_token_unique").on(table.token),
]);

export const encryptionKeys = mysqlTable("encryption_keys", {
	id: int().autoincrement().notNull(),
	keyIdentifier: varchar({ length: 255 }).notNull(),
	encryptedKey: text().notNull(),
	algorithm: varchar({ length: 50 }).default('aes-256-gcm').notNull(),
	purpose: varchar({ length: 100 }).notNull(),
	isActive: tinyint().default(1).notNull(),
	expiresAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	rotatedAt: timestamp({ mode: 'string' }),
},
(table) => [
	index("encryption_keys_identifier_idx").on(table.keyIdentifier),
	index("encryption_keys_purpose_idx").on(table.purpose),
	index("keyIdentifier").on(table.keyIdentifier),
]);

export const engagementLevelHistory = mysqlTable("engagement_level_history", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	oldLevel: varchar({ length: 50 }),
	newLevel: varchar({ length: 50 }).notNull(),
	changedByUserId: int(),
	changedByName: varchar({ length: 255 }),
	reason: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("idx_elh_protocol").on(table.clientProtocolId),
	index("idx_elh_created").on(table.createdAt),
]);

export const enrollmentActivityLog = mysqlTable("enrollment_activity_log", {
	id: int().autoincrement().notNull(),
	enrollmentId: int().notNull(),
	action: mysqlEnum(['enrollment_created','step_toggled','status_changed','payment_recorded','payment_retry','payment_link_sent','notes_updated','enrollment_merged','enrollment_deleted','duplicate_prevented']).notNull(),
	details: text(),
	performedBy: varchar({ length: 255 }),
	performedByUserId: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("activity_log_enrollment_idx").on(table.enrollmentId),
	index("activity_log_action_idx").on(table.action),
	index("activity_log_created_idx").on(table.createdAt),
]);

export const followUpExecutions = mysqlTable("follow_up_executions", {
	id: int().autoincrement().notNull(),
	sequenceId: int("sequence_id").notNull(),
	clientProtocolId: int("client_protocol_id").notNull(),
	triggerEvent: varchar("trigger_event", { length: 100 }).notNull(),
	currentStep: int("current_step").default(1).notNull(),
	status: mysqlEnum(['pending','in_progress','completed','cancelled','failed']).default('pending').notNull(),
	startedAt: timestamp("started_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	completedAt: timestamp("completed_at", { mode: 'string' }),
	nextStepAt: timestamp("next_step_at", { mode: 'string' }),
	errorMessage: text("error_message"),
	createdAt: timestamp("created_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp("updated_at", { mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("sequence_id").on(table.sequenceId),
	index("client_protocol_id").on(table.clientProtocolId),
	index("status").on(table.status),
	index("next_step_at").on(table.nextStepAt),
]);

export const followUpSequenceSteps = mysqlTable("follow_up_sequence_steps", {
	id: int().autoincrement().notNull(),
	sequenceId: int("sequence_id").notNull(),
	stepOrder: int("step_order").notNull(),
	delayHours: int("delay_hours").default(0).notNull(),
	actionType: mysqlEnum("action_type", ['email','task','notification','sms']).notNull(),
	emailSubject: varchar("email_subject", { length: 500 }),
	emailTemplate: text("email_template"),
	taskTitle: varchar("task_title", { length: 255 }),
	taskDescription: text("task_description"),
	isActive: tinyint("is_active").default(1).notNull(),
	createdAt: timestamp("created_at", { mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp("updated_at", { mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("sequence_id").on(table.sequenceId),
	index("step_order").on(table.stepOrder),
]);

export const healthieInvoices = mysqlTable("healthie_invoices", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int(),
	healthiePatientId: varchar({ length: 255 }).notNull(),
	healthieInvoiceId: varchar({ length: 255 }).notNull(),
	amount: decimal({ precision: 10, scale: 2 }).notNull(),
	description: varchar({ length: 500 }),
	status: varchar({ length: 50 }).default('pending').notNull(),
	paidAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const hubLinks = mysqlTable("hub_links", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	url: text().notNull(),
	icon: varchar({ length: 100 }),
	category: mysqlEnum(['platform','course','coaching','resource']).default('platform'),
	isActive: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const incomingSms = mysqlTable("incoming_sms", {
	id: int().autoincrement().notNull(),
	conversationId: int().notNull(),
	phone: varchar({ length: 20 }).notNull(),
	message: text().notNull(),
	ghlMessageId: varchar({ length: 100 }),
	ghlContactId: varchar({ length: 100 }),
	ghlConversationId: varchar({ length: 100 }),
	direction: mysqlEnum(['inbound','outbound']).notNull(),
	status: mysqlEnum(['received','read']).default('received').notNull(),
	clientName: varchar({ length: 255 }),
	receivedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	readAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("incoming_sms_conv_idx").on(table.conversationId),
	index("incoming_sms_phone_idx").on(table.phone),
	index("incoming_sms_direction_idx").on(table.direction),
	index("incoming_sms_received_idx").on(table.receivedAt),
]);

export const intakeFormConfig = mysqlTable("intake_form_config", {
	id: int().autoincrement().notNull(),
	sectionKey: varchar({ length: 100 }).notNull(),
	sectionNumber: int().notNull(),
	title: varchar({ length: 255 }).notNull(),
	displayText: text(),
	isRequired: tinyint().default(1).notNull(),
	requiresSignature: tinyint().default(0).notNull(),
	requiresCheckbox: tinyint().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("intake_config_section_key_idx").on(table.sectionKey),
	index("intake_config_sort_idx").on(table.sortOrder),
	index("sectionKey").on(table.sectionKey),
]);

export const intakeFormResponses = mysqlTable("intake_form_responses", {
	id: int().autoincrement().notNull(),
	enrollmentId: int().notNull(),
	userId: int(),
	status: mysqlEnum(['in_progress','completed','needs_review']).default('in_progress').notNull(),
	currentSection: int().default(1).notNull(),
	completedSections: text(),
	fullName: varchar({ length: 255 }),
	// you can use { mode: 'date' }, if you want to have Date as type for this column
	dateOfBirth: date({ mode: 'string' }),
	sex: mysqlEnum(['female','male','intersex','prefer_not_to_say']),
	email: varchar({ length: 320 }),
	phone: varchar({ length: 50 }),
	streetAddress: text(),
	city: varchar({ length: 255 }),
	stateProvince: varchar({ length: 100 }),
	country: varchar({ length: 100 }).default('United States'),
	zipCode: varchar({ length: 20 }),
	height: varchar({ length: 50 }),
	currentWeight: varchar({ length: 50 }),
	goalWeight: varchar({ length: 50 }),
	bodyFatPercentage: varchar({ length: 50 }),
	peptideGoals: text(),
	primaryGoal: text(),
	secondaryGoal: text(),
	additionalGoals: text(),
	previousPeptideExperience: text(),
	foodCravings: text(),
	currentSupplements: text(),
	medicalIssues: text(),
	physicalActivityRoutine: text(),
	physicalLimitations: text(),
	hormonalStatus: text(),
	currentMedications: text(),
	foodIntolerances: text(),
	digestiveIssues: text(),
	medicalDiagnoses: text(),
	otherGoalSupport: text(),
	aggressivenessScale: int(),
	financialAggressivenessScale: int(),
	organizationalCapacityScale: int(),
	otherConcerns: text(),
	additionalContext: text(),
	referralSource: varchar({ length: 255 }),
	referralName: varchar({ length: 255 }),
	referralOther: varchar({ length: 255 }),
	safetyScreenFlags: text(),
	mentalHealthHistory: text(),
	psychMedications: text(),
	emergencyContactName: varchar({ length: 255 }),
	emergencyContactRelationship: varchar({ length: 100 }),
	emergencyContactPhone: varchar({ length: 50 }),
	alcoholUse: text(),
	nicotineUse: text(),
	cannabisUse: text(),
	otherSubstanceUse: text(),
	sleepDuration: varchar({ length: 50 }),
	sleepQuality: int(),
	stressLevel: int(),
	mainStressors: text(),
	stressManagementMethods: text(),
	wearableDevices: text(),
	typicalMetricsTracked: text(),
	top3Goals: text(),
	weeklyTimeCommitment: varchar({ length: 100 }),
	isMinor: tinyint().default(0).notNull(),
	parentGuardianName: varchar({ length: 255 }),
	startedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	lastSavedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	submittedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("intake_response_enrollment_idx").on(table.enrollmentId),
	index("intake_response_user_idx").on(table.userId),
	index("intake_response_status_idx").on(table.status),
]);

export const intakeFormSignatures = mysqlTable("intake_form_signatures", {
	id: int().autoincrement().notNull(),
	intakeFormId: int().notNull(),
	enrollmentId: int().notNull(),
	sectionKey: varchar({ length: 100 }).notNull(),
	signatureType: mysqlEnum(['drawn','typed']).notNull(),
	signatureData: text().notNull(),
	signedName: varchar({ length: 255 }),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	signedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("intake_sig_form_idx").on(table.intakeFormId),
	index("intake_sig_enrollment_idx").on(table.enrollmentId),
	index("intake_sig_section_idx").on(table.sectionKey),
]);

export const inventoryCategories = mysqlTable("inventory_categories", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	iconUrl: text(),
	accentColor: varchar({ length: 20 }),
	isActive: tinyint().default(1).notNull(),
});

export const inventoryHistory = mysqlTable("inventory_history", {
	id: int().autoincrement().notNull(),
	clientInventoryId: int().notNull(),
	oldStatus: varchar({ length: 20 }),
	newStatus: varchar({ length: 20 }).notNull(),
	changedBy: mysqlEnum(['client','coach','system']).notNull(),
	changedByUserId: int(),
	notes: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("inventory_history_client_inventory_idx").on(table.clientInventoryId),
	index("inventory_history_created_at_idx").on(table.createdAt),
]);

export const inventoryItems = mysqlTable("inventory_items", {
	id: int().autoincrement().notNull(),
	categoryId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	sku: varchar({ length: 100 }),
	quantity: int().default(0).notNull(),
	lowStockThreshold: int().default(5).notNull(),
	price: decimal({ precision: 10, scale: 2 }),
	notes: text(),
	isActive: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	isDiscountable: tinyint().default(0).notNull(),
	pricingTiers: json(),
});

export const inventoryTransactions = mysqlTable("inventory_transactions", {
	id: int().autoincrement().notNull(),
	inventoryItemId: int().notNull(),
	type: mysqlEnum(['sale','restock','adjustment','return']).notNull(),
	quantityChange: int().notNull(),
	previousQuantity: int().notNull(),
	newQuantity: int().notNull(),
	clientProtocolId: int(),
	notes: text(),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("inventory_transactions_item_idx").on(table.inventoryItemId),
	index("inventory_transactions_type_idx").on(table.type),
	index("inventory_transactions_created_idx").on(table.createdAt),
]);

export const journeyNotes = mysqlTable("journey_notes", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	clientProtocolId: int(),
	title: varchar({ length: 255 }),
	content: text().notNull(),
	mood: mysqlEnum(['great','good','okay','struggling','difficult']),
	energyLevel: int(),
	sleepQuality: int(),
	tags: text(),
	isPrivate: tinyint().default(1).notNull(),
	noteDate: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("journey_note_user_idx").on(table.userId),
	index("journey_note_protocol_idx").on(table.clientProtocolId),
	index("journey_note_date_idx").on(table.noteDate),
]);

export const launchpadItemVideos = mysqlTable("launchpad_item_videos", {
	id: int().autoincrement().notNull(),
	launchpadItemId: int().notNull(),
	title: varchar({ length: 255 }).notNull(),
	description: text(),
	videoUrl: text().notNull(),
	videoType: mysqlEnum(['loom','youtube','vimeo','other']).default('loom'),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const launchpadItems = mysqlTable("launchpad_items", {
	id: int().autoincrement().notNull(),
	key: varchar({ length: 100 }).notNull(),
	name: varchar({ length: 255 }).notNull(),
	shortDescription: text(),
	longDescription: text(),
	linkUrl: text(),
	icon: varchar({ length: 100 }),
	category: mysqlEnum(['platform','course','coaching','resource']).default('platform'),
	isActive: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("launchpad_items_key_unique").on(table.key),
]);

export const lifecycleStages = mysqlTable("lifecycle_stages", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 100 }).notNull(),
	description: text(),
	sortOrder: int().default(0).notNull(),
	color: varchar({ length: 20 }).default('#6B7280'),
	icon: varchar({ length: 50 }),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const loginCodes = mysqlTable("login_codes", {
	id: int().autoincrement().notNull(),
	email: varchar({ length: 320 }).notNull(),
	phone: varchar({ length: 50 }),
	code: varchar({ length: 6 }).notNull(),
	expiresAt: timestamp({ mode: 'string' }).notNull(),
	usedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const masterclassVideos = mysqlTable("masterclass_videos", {
	id: int().autoincrement().notNull(),
	sectionNumber: int().notNull(),
	title: varchar({ length: 255 }).notNull(),
	description: text(),
	googleDriveFolderId: varchar({ length: 255 }),
	googleDriveVideoId: varchar({ length: 255 }),
	estimatedDurationMinutes: int(),
	isRequired: tinyint().default(0).notNull(),
	sortOrder: int().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	youtubeVideoId: varchar({ length: 50 }),
	chapters: text(),
},
(table) => [
	index("masterclass_section_idx").on(table.sectionNumber),
	index("masterclass_sort_idx").on(table.sortOrder),
]);

export const notesHistory = mysqlTable("notes_history", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	noteType: mysqlEnum(['internal_notes','coach_notes','comment']).notNull(),
	content: text(),
	commentId: int(),
	changedBy: int(),
	changedByName: varchar({ length: 255 }),
	changeType: mysqlEnum(['created','updated','deleted']).notNull(),
	previousContent: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("notes_history_protocol_idx").on(table.clientProtocolId),
	index("notes_history_type_idx").on(table.noteType),
	index("notes_history_created_at_idx").on(table.createdAt),
]);

export const notificationActivityLog = mysqlTable("notification_activity_log", {
	id: int().autoincrement().notNull(),
	notificationType: varchar({ length: 100 }).notNull(),
	title: varchar({ length: 500 }).notNull(),
	message: text(),
	recipientId: int(),
	recipientName: varchar({ length: 255 }),
	recipientEmail: varchar({ length: 320 }),
	recipientPhone: varchar({ length: 50 }),
	channel: mysqlEnum(['in_app','email','sms','push']).notNull(),
	status: mysqlEnum(['pending','sent','delivered','opened','clicked','failed','bounced']).default('pending').notNull(),
	statusMessage: text(),
	externalId: varchar({ length: 255 }),
	trackingToken: varchar({ length: 64 }),
	clientProtocolId: int(),
	metadata: json(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	sentAt: timestamp({ mode: 'string' }),
	deliveredAt: timestamp({ mode: 'string' }),
	openedAt: timestamp({ mode: 'string' }),
	clickedAt: timestamp({ mode: 'string' }),
},
(table) => [
	index("nal_type_idx").on(table.notificationType),
	index("nal_recipient_idx").on(table.recipientId),
	index("nal_channel_idx").on(table.channel),
	index("nal_status_idx").on(table.status),
	index("nal_created_at_idx").on(table.createdAt),
	index("nal_tracking_token_idx").on(table.trackingToken),
]);

export const notifications = mysqlTable("notifications", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	type: mysqlEnum(['protocol_approved','protocol_viewed','payment_received','payment_failed','payment_refunded','profile_completed','packing_slip_created','protocol_option_selected','protocol_expiring','enrollment_onboarding','consultation_notes_added','consultation_note_reminder','onboarding_automation','low_checkin_score','checkin_submitted','new_store_order','waiver_signed','intake_completed','appointment_booked','appointment_cancelled','client_comment','inventory_out_of_stock','venmo_pending','new_user_registered','referral_submitted','other']).notNull(),
	title: varchar({ length: 255 }).notNull(),
	message: text(),
	clientProtocolId: int(),
	isRead: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("notifications_user_idx").on(table.userId),
	index("notifications_user_read_idx").on(table.userId, table.isRead),
]);

export const onboardingCategories = mysqlTable("onboarding_categories", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	icon: varchar({ length: 50 }),
	description: text(),
	sortOrder: int().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const onboardingOptions = mysqlTable("onboarding_options", {
	id: int().autoincrement().notNull(),
	categoryId: int().notNull(),
	title: varchar({ length: 255 }).notNull(),
	description: text(),
	ctaText: text(),
	linkUrl: text(),
	linkType: mysqlEnum(['internal','external','modal']).default('external').notNull(),
	badge: varchar({ length: 50 }),
	badgeColor: varchar({ length: 20 }),
	icon: varchar({ length: 50 }),
	sortOrder: int().default(0).notNull(),
	isPopular: tinyint().default(0).notNull(),
	isRecommended: tinyint().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const onboardingSettings = mysqlTable("onboarding_settings", {
	id: int().autoincrement().notNull(),
	welcomeTitle: varchar({ length: 255 }).default('Welcome to Omega Longevity').notNull(),
	welcomeSubtitle: text(),
	videoUrl: text(),
	videoPlaceholderText: varchar({ length: 255 }).default('Video coming soon'),
	stepTwoTitle: varchar({ length: 255 }).default('What brings you here today?'),
	stepTwoSubtitle: text(),
	stepThreeTitle: varchar({ length: 255 }).default('Here\'s your action plan'),
	ctaButtonText: varchar({ length: 100 }).default('Let\'s Get Started'),
	persistentButtonText: varchar({ length: 100 }).default('Get Started Guide'),
	isActive: tinyint().default(1).notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const packingSlipAuditLog = mysqlTable("packing_slip_audit_log", {
	id: int().autoincrement().notNull(),
	packingSlipId: int().notNull(),
	action: mysqlEnum(['created','item_added','item_removed','item_status_changed','regenerated','signed','locked','unlocked','bulk_locked','bulk_unlocked','auto_locked','status_changed','tracking_updated','delivery_marked']).notNull(),
	details: text(),
	performedBy: int(),
	performedByName: varchar({ length: 255 }),
	performedByEmail: varchar({ length: 320 }),
	ipAddress: varchar({ length: 45 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("packing_slip_audit_slip_idx").on(table.packingSlipId),
	index("packing_slip_audit_action_idx").on(table.action),
	index("packing_slip_audit_created_at_idx").on(table.createdAt),
	index("packing_slip_audit_performed_by_idx").on(table.performedBy),
]);

export const packingSlipItems = mysqlTable("packing_slip_items", {
	id: int().autoincrement().notNull(),
	packingSlipId: int().notNull(),
	protocolItemId: int().notNull(),
	itemName: varchar({ length: 255 }).notNull(),
	itemType: varchar({ length: 50 }).notNull(),
	quantity: int().default(1).notNull(),
	quantityFulfilled: int().default(0).notNull(),
	quantityBackordered: int().default(0).notNull(),
	status: mysqlEnum(['pending','fulfilled','partial','backordered','cancelled']).default('pending').notNull(),
	price: decimal({ precision: 10, scale: 2 }),
	notes: text(),
	fulfilledAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	shipSource: mysqlEnum(['omega', 'dropship', 'vendor', 'client_sourced']).default('omega'),
	itemTrackingCarrier: varchar({ length: 100 }),
	itemTrackingNumber: varchar({ length: 255 }),
	itemTrackingUrl: varchar({ length: 500 }),
});

export const packingSlips = mysqlTable("packing_slips", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int(),
	protocolOrderId: int(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 320 }).notNull(),
	status: mysqlEnum(['pending','in_progress','partial','complete','cancelled']).default('pending').notNull(),
	totalItems: int().default(0).notNull(),
	itemsFulfilled: int().default(0).notNull(),
	itemsBackordered: int().default(0).notNull(),
	notes: text(),
	fulfilledBy: int(),
	fulfilledByName: varchar({ length: 255 }),
	signatureData: text(),
	signedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	shippingName: varchar({ length: 255 }),
	shippingStreet: varchar({ length: 500 }),
	shippingCity: varchar({ length: 255 }),
	shippingState: varchar({ length: 100 }),
	shippingZip: varchar({ length: 20 }),
	shippingCountry: varchar({ length: 100 }),
	shippingPhone: varchar({ length: 50 }),
	trackingCarrier: varchar({ length: 100 }),
	trackingNumber: varchar({ length: 255 }),
	trackingUrl: varchar({ length: 500 }),
	packageWeight: decimal({ precision: 10, scale: 2 }),
	packageLength: decimal({ precision: 10, scale: 2 }),
	packageWidth: decimal({ precision: 10, scale: 2 }),
	packageHeight: decimal({ precision: 10, scale: 2 }),
	archivedAt: timestamp({ mode: 'string' }),
	archivedBy: int(),
	deliveryStatus: mysqlEnum(['pending','shipped','in_transit','delivered','exception']).default('pending').notNull(),
	deliveredAt: timestamp({ mode: 'string' }),
	deliveryNotificationSent: tinyint().default(0).notNull(),
	isLocked: tinyint().default(0).notNull(),
	lockedAt: timestamp({ mode: 'string' }),
	lockedBy: int(),
	lockedByName: varchar({ length: 255 }),
	storeOrderId: int(),
	source: mysqlEnum(['protocol','store','custom']).default('protocol').notNull(),
	customOrderId: int(),
	contactId: int(),
},
(table) => [
	index("packing_slip_contact_idx").on(table.contactId),
]);

export const pageViews = mysqlTable("page_views", {
	id: int().autoincrement().notNull(),
	path: varchar({ length: 500 }).notNull(),
	referrer: varchar({ length: 1000 }),
	referrerDomain: varchar({ length: 255 }),
	userAgent: varchar({ length: 500 }),
	ipHash: varchar({ length: 64 }),
	country: varchar({ length: 100 }),
	city: varchar({ length: 100 }),
	deviceType: varchar({ length: 20 }),
	browser: varchar({ length: 50 }),
	os: varchar({ length: 50 }),
	screenWidth: int(),
	screenHeight: int(),
	sessionId: varchar({ length: 64 }),
	userId: int(),
	isBot: tinyint().default(0),
	loadTime: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("pv_path_idx").on(table.path),
	index("pv_created_idx").on(table.createdAt),
	index("pv_session_idx").on(table.sessionId),
	index("pv_referrer_domain_idx").on(table.referrerDomain),
	index("pv_device_idx").on(table.deviceType),
]);

export const passwordResetTokens = mysqlTable("password_reset_tokens", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	email: varchar({ length: 320 }).notNull(),
	token: varchar({ length: 64 }).notNull(),
	type: mysqlEnum(['set_password','reset_password']).notNull(),
	expiresAt: timestamp({ mode: 'string' }).notNull(),
	usedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("password_reset_tokens_token_unique").on(table.token),
	index("password_reset_tokens_user_idx").on(table.userId),
	index("password_reset_tokens_token_idx").on(table.token),
	index("password_reset_tokens_email_idx").on(table.email),
]);

export const paymentEvents = mysqlTable("payment_events", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	eventType: mysqlEnum(['payment_due','reminder_sent','payment_received','payment_failed','payment_refunded','payment_cancelled','status_changed']).notNull(),
	amount: decimal({ precision: 10, scale: 2 }),
	paymentMethod: varchar({ length: 50 }),
	transactionId: varchar({ length: 255 }),
	notes: text(),
	performedBy: int().references(() => users.id),
	reminderType: varchar({ length: 50 }),
	emailSentTo: varchar({ length: 320 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	grossAmount: decimal({ precision: 10, scale: 2 }),
	feeAmount: decimal({ precision: 10, scale: 2 }),
	netAmount: decimal({ precision: 10, scale: 2 }),
},
(table) => [
	index("payment_events_client_protocol_idx").on(table.clientProtocolId),
	index("payment_events_event_type_idx").on(table.eventType),
	index("payment_events_created_at_idx").on(table.createdAt),
]);

export const paymentReminderLogs = mysqlTable("payment_reminder_logs", {
	id: bigint({ mode: "number", unsigned: true }).autoincrement().notNull(),
	protocolId: int().notNull().references(() => clientProtocols.id),
	clientName: varchar({ length: 255 }),
	clientEmail: varchar({ length: 255 }),
	reminderType: varchar({ length: 50 }).notNull(),
	reminderDay: int().notNull(),
	sentAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	status: varchar({ length: 50 }).default('sent'),
	errorMessage: text(),
},
(table) => [
	index("id").on(table.id),
]);

export const paypalOrders = mysqlTable("paypal_orders", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	paypalOrderId: varchar({ length: 255 }).notNull(),
	amount: decimal({ precision: 10, scale: 2 }).notNull(),
	currency: varchar({ length: 3 }).default('USD').notNull(),
	status: varchar({ length: 50 }).default('CREATED').notNull(),
	payerEmail: varchar({ length: 320 }),
	payerName: varchar({ length: 255 }),
	transactionId: varchar({ length: 255 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	completedAt: timestamp({ mode: 'string' }),
	grossAmount: decimal("gross_amount", { precision: 10, scale: 2 }),
	feeAmount: decimal("fee_amount", { precision: 10, scale: 2 }),
	netAmount: decimal("net_amount", { precision: 10, scale: 2 }),
},
(table) => [
	index("paypal_orders_paypalOrderId_unique").on(table.paypalOrderId),
]);

export const pendingVenmoPayments = mysqlTable("pending_venmo_payments", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	amount: decimal({ precision: 10, scale: 2 }).notNull(),
	venmoUsername: varchar({ length: 100 }),
	venmoTransactionNote: text(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 320 }),
	status: mysqlEnum(['pending','confirmed','rejected','expired']).default('pending').notNull(),
	verifiedBy: int(),
	verifiedAt: timestamp({ mode: 'string' }),
	verificationNotes: text(),
	rejectionReason: text(),
	expectedVenmoHandle: varchar({ length: 100 }),
	submittedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	expiresAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("pending_venmo_protocol_idx").on(table.clientProtocolId),
	index("pending_venmo_status_idx").on(table.status),
	index("pending_venmo_submitted_idx").on(table.submittedAt),
]);

export const peptideCategories = mysqlTable("peptide_categories", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 100 }).notNull(),
	slug: varchar({ length: 100 }).notNull(),
	description: text(),
	displayOrder: int().default(0).notNull(),
	color: varchar({ length: 20 }),
	icon: varchar({ length: 50 }),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("peptide_categories_slug_unique").on(table.slug),
]);

export const peptideLibrary = mysqlTable("peptide_library", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	purpose: text().notNull(),
	category: varchar({ length: 100 }),
	vialAmount: varchar({ length: 50 }),
	reconstitutionWater: varchar({ length: 100 }),
	recommendedDosage: varchar({ length: 100 }),
	syringeUnits: varchar({ length: 100 }),
	timing: varchar({ length: 100 }),
	frequency: varchar({ length: 100 }),
	duration: varchar({ length: 100 }),
	notes: text(),
	sortOrder: int().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("peptide_category_idx").on(table.category),
	index("peptide_sort_idx").on(table.sortOrder),
]);

export const peptides = mysqlTable("peptides", {
	id: int().autoincrement().notNull(),
	categoryId: int().notNull(),
	name: varchar({ length: 200 }).notNull(),
	slug: varchar({ length: 200 }).notNull(),
	description: text(),
	vialAmount: varchar({ length: 50 }),
	reconstitutionMl: varchar({ length: 50 }),
	dosage: varchar({ length: 100 }),
	syringeUnits: varchar({ length: 50 }),
	timing: varchar({ length: 100 }),
	frequency: varchar({ length: 100 }),
	duration: varchar({ length: 100 }),
	notes: text(),
	formType: varchar({ length: 50 }).default('injectable'),
	productUrl: varchar({ length: 500 }),
	productImageUrl: varchar({ length: 500 }),
	supplierName: varchar({ length: 100 }).default('Limitless Biotech'),
	isActive: tinyint().default(1).notNull(),
	isFeatured: tinyint().default(0).notNull(),
	displayOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	priceRange: varchar({ length: 50 }),
	researchDisclaimer: text(),
	imageUrl: varchar({ length: 500 }),
	pdfUrl: varchar({ length: 500 }),
},
(table) => [
	index("peptides_slug_unique").on(table.slug),
]);

export const productDeletionLog = mysqlTable("product_deletion_log", {
	id: int().autoincrement().notNull(),
	originalProductId: int().notNull(),
	productName: varchar({ length: 255 }).notNull(),
	productData: json().notNull(),
	deletedBy: int().notNull(),
	deletedByName: varchar({ length: 255 }),
	deletionReason: text(),
	affectedClientProtocols: int().default(0).notNull(),
	affectedTemplates: int().default(0).notNull(),
	isRestored: tinyint().default(0).notNull(),
	restoredAt: timestamp({ mode: 'string' }),
	restoredBy: int(),
	restoredProductId: int(),
	deletedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	isDiscountable: tinyint().default(0),
	isActive: tinyint().default(1),
},
(table) => [
	index("deletion_log_original_product_idx").on(table.originalProductId),
	index("deletion_log_deleted_by_idx").on(table.deletedBy),
	index("deletion_log_deleted_at_idx").on(table.deletedAt),
	index("deletion_log_is_restored_idx").on(table.isRestored),
]);

export const productMergeLog = mysqlTable("product_merge_log", {
	id: int().autoincrement().notNull(),
	sourceProductId: int().notNull(),
	sourceProductName: varchar({ length: 255 }).notNull(),
	sourceProductData: json().notNull(),
	targetProductId: int().notNull(),
	targetProductName: varchar({ length: 255 }).notNull(),
	clientProtocolItemsMerged: int().default(0).notNull(),
	templateItemsMerged: int().default(0).notNull(),
	mergedBy: int().notNull(),
	mergedByName: varchar({ length: 255 }),
	mergeReason: text(),
	mergedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("merge_log_source_idx").on(table.sourceProductId),
	index("merge_log_target_idx").on(table.targetProductId),
	index("merge_log_merged_by_idx").on(table.mergedBy),
	index("merge_log_merged_at_idx").on(table.mergedAt),
]);

export const programPhases = mysqlTable("program_phases", {
	id: int().autoincrement().notNull(),
	programId: int().notNull(),
	phaseNumber: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	goals: text(),
	durationMonths: int().default(3).notNull(),
	templateId: int(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const programs = mysqlTable("programs", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	totalMonths: int().default(12).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const progressPhotos = mysqlTable("progress_photos", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	clientProtocolId: int(),
	imageUrl: text().notNull(),
	imageKey: varchar({ length: 500 }).notNull(),
	caption: text(),
	category: mysqlEnum(['before','progress','after','other']).default('progress').notNull(),
	isPrivate: tinyint().default(1).notNull(),
	takenAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("progress_photo_user_idx").on(table.userId),
	index("progress_photo_protocol_idx").on(table.clientProtocolId),
]);

export const projectActivityLog = mysqlTable("project_activity_log", {
	id: int().autoincrement().notNull(),
	clientProjectId: int().notNull(),
	actorTeamMemberId: int(),
	actorName: varchar({ length: 255 }),
	actionType: mysqlEnum(['project_created','project_updated','stage_changed','task_created','task_completed','task_assigned','subtask_created','subtask_completed','subtask_assigned','note_added','status_changed','priority_changed']).notNull(),
	description: text(),
	metadata: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const projectAttachments = mysqlTable("project_attachments", {
	id: int().autoincrement().notNull(),
	clientProjectId: int().notNull(),
	fileName: varchar({ length: 255 }).notNull(),
	fileType: varchar({ length: 100 }),
	fileSize: int(),
	s3Key: varchar({ length: 500 }).notNull(),
	s3Url: text(),
	category: mysqlEnum(['document','image','receipt','packing_slip','lab_results','other']).default('document').notNull(),
	description: text(),
	uploadedByTeamMemberId: int(),
	uploadedByName: varchar({ length: 255 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("attachment_project_idx").on(table.clientProjectId),
	index("attachment_category_idx").on(table.category),
]);

export const projectNotes = mysqlTable("project_notes", {
	id: int().autoincrement().notNull(),
	clientProjectId: int().notNull(),
	authorTeamMemberId: int(),
	authorName: varchar({ length: 255 }),
	noteType: mysqlEnum(['general','decision','handoff','issue','update']).default('general').notNull(),
	content: text().notNull(),
	isPinned: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const projectSubtasks = mysqlTable("project_subtasks", {
	id: int().autoincrement().notNull(),
	projectTaskId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	status: mysqlEnum(['pending','in_progress','completed','blocked','skipped']).default('pending').notNull(),
	assignedTeamMemberId: int(),
	dueDate: timestamp({ mode: 'string' }),
	completedAt: timestamp({ mode: 'string' }),
	completedByTeamMemberId: int(),
	sortOrder: int().default(0).notNull(),
	isRequired: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const projectTasks = mysqlTable("project_tasks", {
	id: int().autoincrement().notNull(),
	clientProjectId: int().notNull(),
	lifecycleStageId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	status: mysqlEnum(['pending','in_progress','completed','blocked','skipped']).default('pending').notNull(),
	assignedTeamMemberId: int(),
	dueDate: timestamp({ mode: 'string' }),
	completedAt: timestamp({ mode: 'string' }),
	completedByTeamMemberId: int(),
	sortOrder: int().default(0).notNull(),
	isRequired: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const projectTrackingInfo = mysqlTable("project_tracking_info", {
	id: int().autoincrement().notNull(),
	clientProjectId: int().notNull(),
	trackingNumber: varchar({ length: 255 }).notNull(),
	carrier: varchar({ length: 100 }),
	carrierUrl: text(),
	description: varchar({ length: 255 }),
	status: mysqlEnum(['pending','in_transit','delivered','exception']).default('pending').notNull(),
	estimatedDelivery: timestamp({ mode: 'string' }),
	deliveredAt: timestamp({ mode: 'string' }),
	createdByTeamMemberId: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("tracking_project_idx").on(table.clientProjectId),
]);

export const promoCodeUsage = mysqlTable("promo_code_usage", {
	id: int().autoincrement().notNull(),
	promoCodeId: int().notNull(),
	enrollmentId: int(),
	userId: int(),
	originalAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	discountAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	finalAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	tier: mysqlEnum(['elite','flagship','essentials','advanced','recovery','immunity','longevity','mitochondria','functional_health_elite']).notNull(),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	usedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("promo_usage_code_idx").on(table.promoCodeId),
	index("promo_usage_enrollment_idx").on(table.enrollmentId),
	index("promo_usage_user_idx").on(table.userId),
]);

export const promoCodes = mysqlTable("promo_codes", {
	id: int().autoincrement().notNull(),
	code: varchar({ length: 50 }).notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	discountType: mysqlEnum(['percent','fixed']).notNull(),
	discountValue: decimal({ precision: 10, scale: 2 }).notNull(),
	maxUses: int(),
	usesCount: int().default(0).notNull(),
	oneTimePerUser: tinyint().default(0).notNull(),
	applicableTiers: text(),
	startsAt: timestamp({ mode: 'string' }),
	expiresAt: timestamp({ mode: 'string' }),
	isActive: tinyint().default(1).notNull(),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("promo_codes_code_idx").on(table.code),
	index("promo_codes_active_idx").on(table.isActive),
]);

export const prospectEngagement = mysqlTable("prospect_engagement", {
	id: int().autoincrement().notNull(),
	prospectId: int().notNull(),
	eventType: mysqlEnum(['sms_link_click','page_view','masterclass_view','tier_view','enrollment_start','enrollment_complete','phone_call','email_sent','email_received','meeting','voicemail','note','status_change','follow_up_scheduled','other']).notNull(),
	url: varchar({ length: 500 }),
	metadata: text(),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	engagementNotes: text("engagement_notes"),
	loggedBy: varchar({ length: 255 }),
	duration: int(),
	outcome: varchar({ length: 255 }),
},
(table) => [
	index("prospect_engagement_prospect_idx").on(table.prospectId),
	index("prospect_engagement_event_type_idx").on(table.eventType),
	index("prospect_engagement_created_at_idx").on(table.createdAt),
]);

export const prospects = mysqlTable("prospects", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	email: varchar({ length: 255 }),
	phone: varchar({ length: 50 }).notNull(),
	status: mysqlEnum(['new','contacted','clicked','viewing','enrolled','declined','stalled','engaged','waiting_on_client','ready_for_consult','not_ready']).default('new').notNull(),
	source: varchar({ length: 100 }),
	notes: text(),
	accessCode: varchar({ length: 100 }),
	trackingToken: varchar({ length: 100 }).notNull(),
	lastContactedAt: timestamp({ mode: 'string' }),
	lastClickedAt: timestamp({ mode: 'string' }),
	lastViewedAt: timestamp({ mode: 'string' }),
	totalSmsSent: int().default(0).notNull(),
	totalClicks: int().default(0).notNull(),
	followUpCount: int().default(0).notNull(),
	nextFollowUpAt: timestamp({ mode: 'string' }),
	followUpPaused: tinyint().default(0).notNull(),
	enrollmentId: int(),
	userId: int(),
	clientId: int(),
	smsOptOut: tinyint().default(0).notNull(),
	assignedTo: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	customStatus: varchar({ length: 100 }),
	thingsToKnow: text(),
	contactId: int(),
},
(table) => [
	index("prospects_status_idx").on(table.status),
	index("prospects_contact_idx").on(table.contactId),
	index("prospects_email_idx").on(table.email),
	index("prospects_phone_idx").on(table.phone),
	index("prospects_tracking_token_idx").on(table.trackingToken),
	index("prospects_next_followup_idx").on(table.nextFollowUpAt),
]);

export const protocolComments = mysqlTable("protocol_comments", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	authorType: mysqlEnum(['coach','client']).notNull(),
	authorName: varchar({ length: 255 }),
	message: text().notNull(),
	loomUrl: text(),
	isRead: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	emailUid: varchar({ length: 100 }),
},
(table) => [
	index("protocol_comments_protocol_idx").on(table.clientProtocolId),
	index("protocol_comments_email_uid_idx").on(table.emailUid),
]);

export const protocolInventoryMapping = mysqlTable("protocol_inventory_mapping", {
	id: int().autoincrement().notNull(),
	protocolItemId: int().notNull(),
	inventoryItemId: int().notNull(),
	quantityPerUnit: int().default(1).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const protocolItems = mysqlTable("protocol_items", {
	id: int().autoincrement().notNull(),
	categoryId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	schedule: text(),
	duration: varchar({ length: 255 }),
	price: decimal({ precision: 10, scale: 2 }).default('0'),
	defaultQty: int().default(0),
	purpose: text(),
	notes: text(),
	affiliateUrl: text(),
	affiliateCode: varchar({ length: 100 }),
	itemType: mysqlEnum(['peptide','supplement','adjunct','supply','service','other']).default('peptide').notNull(),
	isActive: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	isDiscountable: tinyint().default(1).notNull(),
	customCategoryName: varchar({ length: 255 }),
	loomVideoUrl: text(),
	pricingTiers: json(),
	fulfillmentSource: mysqlEnum(['coach','client']).default('coach').notNull(),
	sku: varchar({ length: 20 }),
});

export const protocolOrders = mysqlTable("protocol_orders", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	clientEmail: varchar({ length: 320 }).notNull(),
	clientName: varchar({ length: 255 }).notNull(),
	stripeSessionId: varchar({ length: 255 }),
	stripePaymentIntentId: varchar({ length: 255 }),
	status: mysqlEnum(['pending','completed','failed','refunded']).default('pending').notNull(),
	totalAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	currency: varchar({ length: 3 }).default('usd').notNull(),
	itemsSummary: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	completedAt: timestamp({ mode: 'string' }),
	contactId: int(),
},
(table) => [
	index("protocol_orders_contact_idx").on(table.contactId),
	index("protocol_orders_stripeSessionId_unique").on(table.stripeSessionId),
]);

export const protocolRequirements = mysqlTable("protocol_requirements", {
	id: int().autoincrement().notNull(),
	text: text().notNull(),
	isDefault: tinyint().default(1).notNull(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const protocolSectionTemplates = mysqlTable("protocol_section_templates", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	sectionType: varchar({ length: 50 }).notNull(),
	content: json(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP'),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow(),
});

export const protocolSections = mysqlTable("protocol_sections", {
	id: int().autoincrement().notNull(),
	clientProtocolId: int().notNull(),
	sectionType: mysqlEnum(['periodization','training_split','program_guide']).notNull(),
	isEnabled: tinyint().default(0).notNull(),
	content: json(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("ps_protocol_section_unique").on(table.clientProtocolId, table.sectionType),
	index("ps_protocol_idx").on(table.clientProtocolId),
]);

export const purchases = mysqlTable("purchases", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	coachingPackageId: int().notNull(),
	stripePaymentIntentId: varchar({ length: 255 }),
	stripeSessionId: varchar({ length: 255 }),
	status: mysqlEnum(['pending','completed','failed','refunded']).default('pending').notNull(),
	amountPaid: decimal({ precision: 10, scale: 2 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	completedAt: timestamp({ mode: 'string' }),
});

export const pushNotificationLogs = mysqlTable("push_notification_logs", {
	id: int().autoincrement().notNull(),
	subscriptionId: int().references(() => pushSubscriptions.id),
	title: varchar({ length: 255 }).notNull(),
	body: text(),
	icon: varchar({ length: 500 }),
	url: varchar({ length: 500 }),
	notificationType: mysqlEnum(['protocol_updated','payment_due','payment_received','checkin_available','checkin_reminder','announcement','custom']).notNull(),
	clientProtocolId: int(),
	userId: int(),
	status: mysqlEnum(['pending','sent','delivered','failed','clicked']).default('pending').notNull(),
	errorMessage: text(),
	sentAt: timestamp({ mode: 'string' }),
	deliveredAt: timestamp({ mode: 'string' }),
	clickedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("push_logs_subscription_idx").on(table.subscriptionId),
	index("push_logs_type_idx").on(table.notificationType),
	index("push_logs_status_idx").on(table.status),
	index("push_logs_created_at_idx").on(table.createdAt),
]);

export const pushSubscriptions = mysqlTable("push_subscriptions", {
	id: int().autoincrement().notNull(),
	userId: int(),
	endpoint: text().notNull(),
	p256Dh: varchar({ length: 255 }).notNull(),
	auth: varchar({ length: 255 }).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	clientId: int(),
	userAgent: text(),
	deviceType: mysqlEnum(['mobile','tablet','desktop']).default('desktop'),
	isActive: tinyint().default(1).notNull(),
	lastUsedAt: timestamp({ mode: 'string' }),
	failureCount: int().default(0).notNull(),
	notifyProtocolUpdates: tinyint().default(1).notNull(),
	notifyPaymentDue: tinyint().default(1).notNull(),
	notifyPaymentReceived: tinyint().default(1).notNull(),
	notifyCheckins: tinyint().default(1).notNull(),
	notifyAnnouncements: tinyint().default(1).notNull(),
},
(table) => [
	index("push_sub_user_idx").on(table.userId),
	index("push_subscriptions_client_idx").on(table.clientId),
	index("push_subscriptions_active_idx").on(table.isActive),
]);

export const recipientTracking = mysqlTable("recipient_tracking", {
	id: int().autoincrement().notNull(),
	announcementId: int().notNull(),
	recipientEmail: varchar({ length: 320 }).notNull(),
	trackingId: varchar({ length: 64 }).notNull(),
	opened: tinyint().default(0).notNull(),
	openedAt: timestamp({ mode: 'string' }),
	clicked: tinyint().default(0).notNull(),
	clickedAt: timestamp({ mode: 'string' }),
	clickCount: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("recipient_tracking_announcement_idx").on(table.announcementId),
	index("recipient_tracking_email_idx").on(table.recipientEmail),
	index("recipient_tracking_id_idx").on(table.trackingId),
	index("trackingId").on(table.trackingId),
]);

export const referrals = mysqlTable("referrals", {
	id: int().autoincrement().notNull(),
	referrerId: int().notNull(),
	referredUserId: int(),
	referredEmail: varchar({ length: 320 }),
	status: mysqlEnum(['pending','signed_up','purchased','rewarded']).default('pending').notNull(),
	rewardAmount: decimal({ precision: 10, scale: 2 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	convertedAt: timestamp({ mode: 'string' }),
	programTier: mysqlEnum(['essentials','transformation','elite']),
	programAmount: decimal({ precision: 10, scale: 2 }),
	commissionRate: decimal({ precision: 5, scale: 4 }),
	commissionAmount: decimal({ precision: 10, scale: 2 }),
	paidAt: timestamp({ mode: 'string' }),
});

export const refundRequests = mysqlTable("refund_requests", {
	id: int().autoincrement().notNull(),
	protocolId: int().notNull(),
	clientId: varchar({ length: 255 }).notNull(),
	reason: text().notNull(),
	status: mysqlEnum(['pending','approved','rejected','processed']).default('pending'),
	refundAmount: varchar({ length: 50 }),
	adminNotes: text(),
	requestedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP'),
	reviewedAt: timestamp({ mode: 'string' }),
	processedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP'),
	updatedAt: timestamp({ mode: 'string' }).onUpdateNow(),
},
(table) => [
	index("refund_protocol_idx").on(table.protocolId),
	index("refund_client_idx").on(table.clientId),
	index("refund_status_idx").on(table.status),
]);

export const revenueGoals = mysqlTable("revenue_goals", {
	id: int().autoincrement().notNull(),
	year: int().notNull(),
	month: int().notNull(),
	targetAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	notes: text(),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("revenue_goals_year_month_idx").on(table.year, table.month),
]);

export const savedAddresses = mysqlTable("saved_addresses", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	label: varchar({ length: 100 }).notNull(),
	isDefault: tinyint().default(0).notNull(),
	name: varchar({ length: 255 }).notNull(),
	street: varchar({ length: 500 }).notNull(),
	street2: varchar({ length: 255 }),
	city: varchar({ length: 255 }).notNull(),
	state: varchar({ length: 100 }).notNull(),
	zip: varchar({ length: 20 }).notNull(),
	country: varchar({ length: 100 }).default('United States').notNull(),
	countryCode: varchar({ length: 10 }).default('US').notNull(),
	phone: varchar({ length: 50 }),
	isVerified: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("saved_addresses_user_idx").on(table.userId),
	index("saved_addresses_user_default_idx").on(table.userId, table.isDefault),
]);

export const securityEvents = mysqlTable("security_events", {
	id: int().autoincrement().notNull(),
	userId: int(),
	eventType: varchar({ length: 100 }).notNull(),
	severity: varchar({ length: 20 }).default('info').notNull(),
	description: text(),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	metadata: json(),
	resolved: tinyint().default(0).notNull(),
	resolvedAt: timestamp({ mode: 'string' }),
	resolvedBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("security_events_user_idx").on(table.userId),
	index("security_events_type_idx").on(table.eventType),
	index("security_events_severity_idx").on(table.severity),
	index("security_events_created_at_idx").on(table.createdAt),
]);

export const siteSettings = mysqlTable("site_settings", {
	id: int().autoincrement().notNull(),
	key: varchar({ length: 100 }).notNull(),
	value: text(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("site_settings_key_unique").on(table.key),
]);

export const smsConversations = mysqlTable("sms_conversations", {
	id: int().autoincrement().notNull(),
	phone: varchar({ length: 20 }).notNull(),
	ghlContactId: varchar({ length: 100 }),
	ghlConversationId: varchar({ length: 100 }),
	clientName: varchar({ length: 255 }),
	clientEmail: varchar({ length: 320 }),
	lastMessageAt: timestamp({ mode: 'string' }),
	lastMessagePreview: varchar({ length: 160 }),
	unreadCount: int().default(0).notNull(),
	isArchived: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("sms_conversations_phone_unique").on(table.phone),
	index("sms_conv_phone_idx").on(table.phone),
	index("sms_conv_last_msg_idx").on(table.lastMessageAt),
	index("sms_conv_unread_idx").on(table.unreadCount),
]);

export const smsMessageLog = mysqlTable("sms_message_log", {
	id: int().autoincrement().notNull(),
	phone: varchar({ length: 20 }).notNull(),
	message: text().notNull(),
	messageType: mysqlEnum(['appointment_reminder_24h','appointment_reminder_1h','booking_confirmation','cancellation_notification','payment_reminder','test','manual','bulk_announcement','other']).notNull(),
	status: mysqlEnum(['pending','sent','delivered','failed']).default('pending').notNull(),
	ghlMessageId: varchar({ length: 100 }),
	ghlConversationId: varchar({ length: 100 }),
	ghlContactId: varchar({ length: 100 }),
	errorMessage: text(),
	appointmentId: int(),
	clientProtocolId: int(),
	clientName: varchar({ length: 255 }),
	clientEmail: varchar({ length: 320 }),
	sentBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	sentAt: timestamp({ mode: 'string' }),
	scheduledFor: timestamp({ mode: 'string' }),
	isScheduled: tinyint().default(0).notNull(),
},
(table) => [
	index("sms_phone_idx").on(table.phone),
	index("sms_type_idx").on(table.messageType),
	index("sms_status_idx").on(table.status),
	index("sms_created_idx").on(table.createdAt),
]);

export const smsMessages = mysqlTable("sms_messages", {
	id: int().autoincrement().notNull(),
	prospectId: int(),
	userId: int(),
	toPhone: varchar({ length: 50 }).notNull(),
	fromPhone: varchar({ length: 50 }).notNull(),
	body: text().notNull(),
	twilioSid: varchar({ length: 100 }),
	status: mysqlEnum(['queued','sending','sent','delivered','undelivered','failed','not_configured']).default('queued').notNull(),
	errorCode: varchar({ length: 20 }),
	errorMessage: text(),
	direction: mysqlEnum(['outbound','inbound']).default('outbound').notNull(),
	templateKey: varchar({ length: 100 }),
	price: varchar({ length: 20 }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("sms_messages_prospect_idx").on(table.prospectId),
	index("sms_messages_user_idx").on(table.userId),
	index("sms_messages_twilio_sid_idx").on(table.twilioSid),
	index("sms_messages_status_idx").on(table.status),
	index("sms_messages_created_at_idx").on(table.createdAt),
]);

export const smsSettings = mysqlTable("sms_settings", {
	id: int().autoincrement().notNull(),
	settingKey: varchar({ length: 100 }).notNull(),
	settingValue: text().notNull(),
	description: text(),
	updatedBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("sms_settings_settingKey_unique").on(table.settingKey),
]);

export const smsTemplates = mysqlTable("sms_templates", {
	id: int().autoincrement().notNull(),
	templateKey: varchar({ length: 100 }).notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	body: text().notNull(),
	isActive: tinyint().default(1).notNull(),
	isDefault: tinyint().default(0).notNull(),
	category: mysqlEnum(['initial_outreach','follow_up','reminder','custom']).default('custom').notNull(),
	sendAfterHours: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("sms_templates_key_idx").on(table.templateKey),
	index("sms_templates_category_idx").on(table.category),
]);

export const storeOrderItems = mysqlTable("store_order_items", {
	id: int().autoincrement().notNull(),
	storeOrderId: int().notNull(),
	inventoryItemId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	quantity: int().notNull(),
	pricePerUnit: decimal({ precision: 10, scale: 2 }).notNull(),
	isDiscountable: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("store_order_item_order_idx").on(table.storeOrderId),
	index("store_order_item_inventory_idx").on(table.inventoryItemId),
]);

export const storeOrders = mysqlTable("store_orders", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	paypalOrderId: varchar({ length: 255 }),
	venmoTransactionId: varchar({ length: 255 }),
	paymentMethod: mysqlEnum(['paypal','venmo','stripe','manual']).notNull(),
	subtotal: decimal({ precision: 10, scale: 2 }).notNull(),
	discountAmount: decimal({ precision: 10, scale: 2 }).default('0').notNull(),
	shippingFee: decimal({ precision: 10, scale: 2 }).default('0.00').notNull(),
	total: decimal({ precision: 10, scale: 2 }).notNull(),
	status: mysqlEnum(['pending','paid','processing','shipped','delivered','cancelled','refunded']).default('pending').notNull(),
	payerEmail: varchar({ length: 320 }),
	payerName: varchar({ length: 255 }),
	notes: text(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	paidAt: timestamp({ mode: 'string' }),
	trackingNumber: varchar({ length: 255 }),
	trackingCarrier: varchar({ length: 100 }),
	shippedAt: timestamp({ mode: 'string' }),
	shippingName: varchar({ length: 255 }),
	shippingStreet: varchar({ length: 500 }),
	shippingCity: varchar({ length: 255 }),
	shippingState: varchar({ length: 100 }),
	shippingZip: varchar({ length: 20 }),
	shippingCountry: varchar({ length: 100 }),
	shippingPhone: varchar({ length: 50 }),
},
(table) => [
	index("store_order_user_idx").on(table.userId),
	index("store_order_status_idx").on(table.status),
	index("store_order_paypal_idx").on(table.paypalOrderId),
]);

export const storePromoCodeUsage = mysqlTable("store_promo_code_usage", {
	id: int().autoincrement().notNull(),
	promoCodeId: int().notNull(),
	orderId: int(),
	userId: int(),
	originalAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	discountAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	finalAmount: decimal({ precision: 10, scale: 2 }).notNull(),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	usedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("store_promo_usage_code_idx").on(table.promoCodeId),
	index("store_promo_usage_order_idx").on(table.orderId),
	index("store_promo_usage_user_idx").on(table.userId),
]);

export const storePromoCodes = mysqlTable("store_promo_codes", {
	id: int().autoincrement().notNull(),
	code: varchar({ length: 50 }).notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	discountType: mysqlEnum(['percent','fixed']).notNull(),
	discountValue: decimal({ precision: 10, scale: 2 }).notNull(),
	minimumOrderAmount: decimal({ precision: 10, scale: 2 }),
	maxUses: int(),
	usesCount: int().default(0).notNull(),
	oneTimePerUser: tinyint().default(0).notNull(),
	applicableCategories: text(),
	startsAt: timestamp({ mode: 'string' }),
	expiresAt: timestamp({ mode: 'string' }),
	isActive: tinyint().default(1).notNull(),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("store_promo_codes_code_idx").on(table.code),
	index("store_promo_codes_active_idx").on(table.isActive),
]);

export const storeWaivers = mysqlTable("store_waivers", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	firstName: varchar({ length: 100 }).notNull(),
	lastName: varchar({ length: 100 }).notNull(),
	email: varchar({ length: 320 }).notNull(),
	phone: varchar({ length: 50 }).notNull(),
	parentGuardianName: varchar({ length: 200 }),
	signatureData: text().notNull(),
	agreedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	ipAddress: varchar({ length: 45 }),
	expiresAt: timestamp({ mode: 'string' }),
	renewalReminderSent: tinyint().default(0).notNull(),
	renewalToken: varchar({ length: 64 }),
},
(table) => [
	index("store_waivers_renewalToken_unique").on(table.renewalToken),
]);

export const teamMembers = mysqlTable("team_members", {
	id: int().autoincrement().notNull(),
	userId: int(),
	name: varchar({ length: 255 }).notNull(),
	email: varchar({ length: 320 }),
	roleId: int(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const teamNotifications = mysqlTable("team_notifications", {
	id: int().autoincrement().notNull(),
	teamMemberId: int().notNull(),
	type: mysqlEnum(['task_assigned','task_overdue','subtask_assigned','subtask_overdue','project_assigned','deadline_approaching','mention']).notNull(),
	title: varchar({ length: 255 }).notNull(),
	message: text(),
	clientProjectId: int(),
	projectTaskId: int(),
	projectSubtaskId: int(),
	isRead: tinyint().default(0).notNull(),
	isEmailSent: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const teamRoles = mysqlTable("team_roles", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 100 }).notNull(),
	description: text(),
	color: varchar({ length: 20 }).default('#6B7280'),
	sortOrder: int().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const templateItems = mysqlTable("template_items", {
	id: int().autoincrement().notNull(),
	templateId: int().notNull(),
	protocolItemId: int().notNull(),
	quantity: int().default(1).notNull(),
	isRecommended: tinyint().default(1).notNull(),
	customNotes: text(),
	sortOrder: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const templates = mysqlTable("templates", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	durationMonths: int().default(3).notNull(),
	isDefault: tinyint().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	hidePricing: tinyint().default(0).notNull(),
	autoSync: tinyint().default(0).notNull(),
	tags: text(),
});

export const transformationAccessCodes = mysqlTable("transformation_access_codes", {
	id: int().autoincrement().notNull(),
	code: varchar({ length: 100 }).notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	usageLimit: int(),
	usageCount: int().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	expiresAt: timestamp({ mode: 'string' }),
	createdBy: int(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	tier: mysqlEnum(['elite','flagship','essentials','advanced','recovery','immunity','longevity','mitochondria','functional_health_elite']),
},
(table) => [
	index("transformation_codes_code_idx").on(table.code),
	index("transformation_codes_active_idx").on(table.isActive),
]);

export const transformationCodeUsage = mysqlTable("transformation_code_usage", {
	id: int().autoincrement().notNull(),
	codeId: int().notNull(),
	userId: int(),
	clientId: int(),
	email: varchar({ length: 320 }),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	usedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("code_usage_code_idx").on(table.codeId),
	index("code_usage_user_idx").on(table.userId),
	index("code_usage_used_at_idx").on(table.usedAt),
]);

export const transformationEnrollments = mysqlTable("transformation_enrollments", {
	id: int().autoincrement().notNull(),
	userId: int(),
	clientId: int(),
	accessCodeId: int(),
	programType: mysqlEnum(['90_day_transformation','protocol_only']).default('90_day_transformation').notNull(),
	status: mysqlEnum(['enrolled','watching_videos','video_complete','coaching_paid','intake_complete','discovery_scheduled','discovery_complete','protocol_preparing','protocol_review','protocol_paid','launched','fulfillment','shipped','delivered','training_scheduled','training_complete','active','week3_review','month2','month3_final','completed','renewed']).default('enrolled').notNull(),
	coachingFeePaid: tinyint().default(0).notNull(),
	coachingFeeAmount: decimal({ precision: 10, scale: 2 }),
	coachingFeePaidAt: timestamp({ mode: 'string' }),
	coachingFeeStripePaymentId: varchar({ length: 255 }),
	protocolCostPaid: tinyint().default(0).notNull(),
	protocolCostAmount: decimal({ precision: 10, scale: 2 }),
	protocolCostPaidAt: timestamp({ mode: 'string' }),
	protocolCostStripePaymentId: varchar({ length: 255 }),
	clientProtocolId: int(),
	discoverySessionScheduledAt: timestamp({ mode: 'string' }),
	discoverySessionCompletedAt: timestamp({ mode: 'string' }),
	reconstitutionSessionScheduledAt: timestamp({ mode: 'string' }),
	reconstitutionSessionCompletedAt: timestamp({ mode: 'string' }),
	week3ReviewScheduledAt: timestamp({ mode: 'string' }),
	week3ReviewCompletedAt: timestamp({ mode: 'string' }),
	month2SessionScheduledAt: timestamp({ mode: 'string' }),
	month2SessionCompletedAt: timestamp({ mode: 'string' }),
	finalReviewScheduledAt: timestamp({ mode: 'string' }),
	finalReviewCompletedAt: timestamp({ mode: 'string' }),
	boxShippedAt: timestamp({ mode: 'string' }),
	boxDeliveredAt: timestamp({ mode: 'string' }),
	trackingNumber: varchar({ length: 255 }),
	omegaEliteActivatedAt: timestamp({ mode: 'string' }),
	omegaEliteExpiresAt: timestamp({ mode: 'string' }),
	biologicalAgeTestOrdered: tinyint().default(0).notNull(),
	biologicalAgeTestOrderedAt: timestamp({ mode: 'string' }),
	biologicalAgeResult: decimal({ precision: 5, scale: 2 }),
	biologicalAgeResultAt: timestamp({ mode: 'string' }),
	coachNotes: text(),
	enrolledAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	launchedAt: timestamp({ mode: 'string' }),
	completedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	tier: mysqlEnum(['elite','flagship','essentials','advanced','recovery','immunity','longevity','mitochondria','functional_health_elite']).default('flagship').notNull(),
	intakeFormCompleted: tinyint().default(0).notNull(),
	intakeFormCompletedAt: timestamp({ mode: 'string' }),
	email: varchar({ length: 255 }),
	clientName: varchar({ length: 255 }),
	authToken: varchar({ length: 255 }),
	authTokenExpiresAt: timestamp({ mode: 'string' }),
	followUpReminderSentAt: timestamp({ mode: 'string' }),
	stalledNotificationSentAt: datetime({ mode: 'string'}),
	phone: varchar({ length: 50 }),
	shippingName: varchar({ length: 255 }),
	shippingStreet: varchar({ length: 500 }),
	shippingCity: varchar({ length: 255 }),
	shippingState: varchar({ length: 100 }),
	shippingZip: varchar({ length: 20 }),
	shippingCountry: varchar({ length: 100 }).default('United States'),
	profileCompleted: tinyint().default(0),
	profileCompletedAt: datetime({ mode: 'string'}),
	intakeReminder24HSentAt: datetime({ mode: 'string'}),
	intakeReminder72HSentAt: datetime({ mode: 'string'}),
	contactId: int(),
	vipConcierge: tinyint().default(0).notNull(),
	vipConciergeFee: decimal({ precision: 10, scale: 2 }),
},
(table) => [
	index("transformation_enroll_user_idx").on(table.userId),
	index("transformation_enroll_contact_idx").on(table.contactId),
	index("transformation_enroll_client_idx").on(table.clientId),
	index("transformation_enroll_status_idx").on(table.status),
	index("transformation_enroll_protocol_idx").on(table.clientProtocolId),
]);

export const transformationLabs = mysqlTable("transformation_labs", {
	id: int().autoincrement().notNull(),
	enrollmentId: int().notNull(),
	fileName: varchar({ length: 255 }).notNull(),
	fileUrl: text().notNull(),
	fileSize: int(),
	fileType: varchar({ length: 100 }),
	labType: varchar({ length: 255 }),
	labDate: timestamp({ mode: 'string' }),
	labProvider: varchar({ length: 255 }),
	notes: text(),
	reviewedBy: int(),
	reviewedAt: timestamp({ mode: 'string' }),
	reviewNotes: text(),
	uploadedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("transformation_labs_enrollment_idx").on(table.enrollmentId),
]);

export const transformationPendingPayments = mysqlTable("transformation_pending_payments", {
	id: bigint({ mode: "number", unsigned: true }).autoincrement().notNull(),
	enrollmentId: int().notNull(),
	clientName: varchar({ length: 255 }).notNull(),
	clientEmail: varchar({ length: 255 }),
	tier: varchar({ length: 50 }).notNull(),
	amount: decimal({ precision: 10, scale: 2 }).notNull(),
	paymentMethod: varchar({ length: 50 }).default('venmo').notNull(),
	venmoUsername: varchar({ length: 255 }),
	promoCodeId: int(),
	promoCode: varchar({ length: 100 }),
	originalAmount: decimal({ precision: 10, scale: 2 }),
	discountAmount: decimal({ precision: 10, scale: 2 }),
	status: varchar({ length: 50 }).default('pending').notNull(),
	adminNotes: text(),
	verifiedBy: int(),
	verifiedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP'),
	updatedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP'),
},
(table) => [
	index("id").on(table.id),
]);

export const userFavoritePeptides = mysqlTable("user_favorite_peptides", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	peptideId: int().notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("user_favorite_peptides_user_idx").on(table.userId),
	index("user_favorite_peptides_peptide_idx").on(table.peptideId),
	index("user_peptide_unique").on(table.userId, table.peptideId),
]);

export const userFavorites = mysqlTable("user_favorites", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	inventoryItemId: int().notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const userOnboardingStatus = mysqlTable("user_onboarding_status", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	hasCompletedOnboarding: tinyint().default(0).notNull(),
	selectedOptionIds: text(),
	completedAt: timestamp({ mode: 'string' }),
	lastViewedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const userSessions = mysqlTable("user_sessions", {
	id: int().autoincrement().notNull(),
	userId: int().notNull(),
	sessionToken: varchar({ length: 512 }).notNull(),
	deviceInfo: text(),
	ipAddress: varchar({ length: 45 }),
	userAgent: text(),
	lastActiveAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	expiresAt: timestamp({ mode: 'string' }).notNull(),
	isRevoked: tinyint().default(0).notNull(),
	revokedAt: timestamp({ mode: 'string' }),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
},
(table) => [
	index("user_sessions_user_idx").on(table.userId),
	index("user_sessions_token_idx").on(table.sessionToken),
	index("user_sessions_expires_idx").on(table.expiresAt),
	index("sessionToken").on(table.sessionToken),
]);

export const users = mysqlTable("users", {
	id: int().autoincrement().notNull(),
	openId: varchar({ length: 64 }).notNull(),
	name: text(),
	email: varchar({ length: 320 }),
	loginMethod: varchar({ length: 64 }),
	role: mysqlEnum(['user','admin','manager','viewer','finance']).default('user').notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
	lastSignedIn: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	receiveNotifications: tinyint().default(0).notNull(),
	stripeCustomerId: varchar({ length: 255 }),
	referralCode: varchar({ length: 32 }),
	referredBy: int(),
	permissions: json(),
	phone: varchar({ length: 20 }),
	receiveSmsNotifications: tinyint().default(0).notNull(),
	waiverBypassedAt: timestamp({ mode: 'string' }),
	waiverBypassedBy: int(),
	waiverBypassReason: text(),
	notificationEmail: varchar({ length: 320 }),
	enabledNotificationTypes: text(),
	enabledEmailNotificationTypes: text(),
	digestFrequency: mysqlEnum(['none','daily','weekly']).default('none'),
	digestSendTime: varchar({ length: 5 }).default('09:00'),
	digestLastSentAt: timestamp({ mode: 'string' }),
	pushSubscription: text(),
	pushEnabledTypes: text(),
	enabledSmsNotificationTypes: text(),
	lastSeenAt: timestamp({ mode: 'string' }),
	passwordHash: varchar({ length: 255 }),
	emailVerified: tinyint().default(0).notNull(),
	contactId: int(),
},
(table) => [
	index("users_openId_unique").on(table.openId),
	index("users_contact_idx").on(table.contactId),
	index("users_email_idx").on(table.email),
	index("users_role_idx").on(table.role),
	index("users_referral_code_idx").on(table.referralCode),
	index("users_email_unique").on(table.email),
]);

export const videoProgress = mysqlTable("video_progress", {
	id: int().autoincrement().notNull(),
	enrollmentId: int().notNull(),
	videoId: int().notNull(),
	watchedSeconds: int().default(0).notNull(),
	totalSeconds: int(),
	percentComplete: decimal({ precision: 5, scale: 2 }).default('0').notNull(),
	isCompleted: tinyint().default(0).notNull(),
	completedAt: timestamp({ mode: 'string' }),
	lastWatchedAt: timestamp({ mode: 'string' }),
	watchCount: int().default(0).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("video_progress_enrollment_idx").on(table.enrollmentId),
	index("video_progress_video_idx").on(table.videoId),
	index("video_progress_enroll_video_idx").on(table.enrollmentId, table.videoId),
]);

export const waiverRenewalHistory = mysqlTable("waiver_renewal_history", {
	id: int().autoincrement().notNull(),
	waiverId: int().notNull(),
	renewedAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	previousExpiresAt: timestamp({ mode: 'string' }),
	newExpiresAt: timestamp({ mode: 'string' }),
	ipAddress: varchar({ length: 45 }),
});

export const workflowTemplateSubtasks = mysqlTable("workflow_template_subtasks", {
	id: int().autoincrement().notNull(),
	workflowTemplateTaskId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	defaultOwnerRoleId: int(),
	defaultAssignedTeamMemberId: int(),
	sortOrder: int().default(0).notNull(),
	isRequired: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const workflowTemplateTasks = mysqlTable("workflow_template_tasks", {
	id: int().autoincrement().notNull(),
	workflowTemplateId: int().notNull(),
	lifecycleStageId: int().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	defaultOwnerRoleId: int(),
	sortOrder: int().default(0).notNull(),
	dueDaysFromStart: int(),
	isRequired: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
});

export const workflowTemplates = mysqlTable("workflow_templates", {
	id: int().autoincrement().notNull(),
	name: varchar({ length: 255 }).notNull(),
	description: text(),
	durationDays: int().default(90).notNull(),
	isDefault: tinyint().default(0).notNull(),
	isActive: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
});

export const teamNotificationPreferences = mysqlTable("team_notification_preferences", {
	id: int().autoincrement().notNull(),
	teamMemberId: int().notNull(),
	taskAssignedEmail: tinyint().default(1).notNull(),
	taskOverdueEmail: tinyint().default(1).notNull(),
	projectAssignedEmail: tinyint().default(1).notNull(),
	deadlineApproachingEmail: tinyint().default(1).notNull(),
	escalationEmail: tinyint().default(1).notNull(),
	digestEmail: tinyint().default(1).notNull(),
	mentionEmail: tinyint().default(1).notNull(),
	pipelineUpdateEmail: tinyint().default(0).notNull(),
	fulfillmentAlertEmail: tinyint().default(1).notNull(),
	createdAt: timestamp({ mode: 'string' }).default('CURRENT_TIMESTAMP').notNull(),
	updatedAt: timestamp({ mode: 'string' }).defaultNow().onUpdateNow().notNull(),
},
(table) => [
	index("team_notif_prefs_member_idx").on(table.teamMemberId),
]);
