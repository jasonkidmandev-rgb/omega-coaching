// VenmoVerificationQueue removed - migrating to Stripe
// This placeholder prevents import errors from pages that still reference it

export function VenmoVerificationQueue() {
  return null;
}

export default VenmoVerificationQueue;
